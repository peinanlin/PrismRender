## 1. P0 — 固定完成版基线与验证入口

- [x] 1.1 确认 HPWater 38/38 对应源码、现有工作区修改和重叠任务，建立可恢复的基线；验证：在 `docs/ARCHITECTURE_REFACTOR_ACCEPTANCE.md` 记录 revision 或文件 hash manifest、恢复方法及重叠写入检查结果，Git 不可用时取得认可的恢复点后再推进。
- [x] 1.2 盘点 Host 初始化/主循环/退出调用顺序、Feature 作用域、RenderScene 可变入口和生产源归属；验证：验收文档包含调用轨迹、成员 owner 表、mutation 来源表和 target 清单，明确 Fluid 的当前作用域，不只统计文件行数。
- [x] 1.3 新增 `scripts/Validate-ArchitectureRefactor.ps1`，统一 phase/backend/binary/output-root 输入、串行 GPU 执行、失败退出和证据索引；验证：dry-run 展示正确命令，故意缺文件/子进程失败时返回失败，所有输出限制在独立 architecture-refactor 目录。
- [x] 1.4 为 HPWater 捕获/汇总工具接入独立二进制和输出目录；仅缺少参数时增加兼容参数；验证：默认调用行为及旧报告字段不变，隔离调用不写入 `artifacts/hpwater-validation/`，两次运行不覆盖同一批证据。
- [x] 1.5 建立同后端前后比较与跨后端 parity 的独立检查入口，记录图像、Pass/资源状态、共享模拟次数及历史重置；验证：相同样本通过，超过 design V2 门限的人工差异样本失败，跨后端宽门限不能用于同后端比较。
- [x] 1.6 在当前源码运行现有 Windows 构建/CPU/GPU 基线并登记可用平台；验证：保存 windows-ci 与 windows-vulkan-only-ci 结果，Windows GPU 使用显式 gpu 标签串行运行，既有 warning/error 独立标记而不冒充通过；其他平台只作可选兼容性记录，不构成本 change 门禁。
- [x] 1.7 捕获 HPWater 七视角、三质量、两尺寸及 lifecycle/temporal 基线，并建立全部 20 个 Demo 固定输入图像基线；验证：逐项记录相机、seed/时间步长、流送就绪、尺寸、view/queue/config，关键图像至少重复三次确认稳定，保存水线、resize、reset、scene switch 序列而非仅单帧。
- [x] 1.8 按修订后的 design V3 固定可重放性能特征基线；验证：记录 Editor 双视图、同 EXE Game-only、Standalone、空负载和完整 Demo 的 build/backend/validation/profiling level/active views、CPU lane、GPU、等待、内存与 PSO 条件，同进程 A/B 和至少三次独立进程可重放；GPU DVFS/显示状态如实记录但不要求管理员锁频、机器级 Vulkan 注册或跨批绝对 GPU 一致才进入结构阶段。
- [x] 1.9 固定 DemoSceneCatalog 全覆盖清单及连续帧配置；验证：20 个 key 无遗漏，三个 Ocean、全部四个 Fluid、TAA/阴影/流送/双视图具有连续采样和控制序列，新增目录项未配基线时失败，不适用配置有明确说明。
- [x] 1.10 记录线程亲和和跨帧引用现状；验证：backend/context、工厂上传、PSO/descriptor/cache、ImGui/GLFW、环境配置和 capture 帧身份逐项标记 owner/调用线程/保活方法，区分 Asset IO、逐 Pass async 与 GPU async compute。

P0 实际阻碍（2026-08-28，详见验收记录）：原源码 windows-vulkan-only-ci 在 UI 符号链接处失败；原 lights Demo 的 D3D12 描述符堆创建失败可独立复现，Vulkan 则出现子资源 image layout 校验错误。1.6–1.9 未通过，不进入 P1；这些既有问题不能伪记为重构回归或靠降画质/关闭校验绕过。

用户已确认继续修复上述 P0 前置问题；以下修复独立于后续架构迁移验收：

- [x] 1.11 修复无 Editor 构建的应用/UI 编译边界；验证：windows-vulkan-only-ci configure/build/CPU 测试和无 Editor Vulkan 实际捕获通过，不链接 ImGui；完整 Windows/Editor 构建与原图像对照保持。
- [x] 1.12 定位并修复 lights D3D12 texture-view descriptor heap 分配失败及 sampler heap 耗尽；验证：相同完整 sampler 表不可变复用、修改不污染其他集合、按 fence 退役；保留默认画质/对象/视图与资源安全寿命，增加分配/回收回归测试，lights 原失败配置及 HPWater/原可运行 Demo 通过。
- [x] 1.13 定位并修复 lights Vulkan 多 mip/layer 采样布局失配及关闭天空 LUT 绑定；验证：已初始化海洋数组/天空 2D 后备、天空关闭/开启/关闭和每视图帧槽绑定回归通过，lights native/serial 与 HPWater validation 无新增错误，不关闭校验或隐藏未初始化采样。
- [x] 1.14 保存前置修复证据及独立修复后恢复点，重跑受影响基线；验证：原始快照/既有 golden 不覆盖，修复和纯结构阶段分开归因，实际未通过的 P0 矩阵保持未完成。
- [x] 1.15 定位并修复完整 FFT GPU fixture 严格校验中的资源初态/屏障/呈现错误；验证：双后端完整 fixture 显式开启可用校验层，数值断言保持且无 validation error，不能以单独 fallback fixture 或未开校验的结果替代。
- [x] 1.16 修复 SSR 在 PlanarReflections 关闭时绑定未初始化纹理的问题；验证：已初始化后备、与 producer 一致的每视图/当前帧槽选择、关闭/开启/关闭及尺寸重建双后端严格校验通过；保留默认 deferred+SSR fixture、开启算法和 Demo 设置，原二进制同后端图像对照通过。
- [x] 1.17 统一审计并修复 GTAO 等可选 Feature 在关闭状态的无效资源绑定；验证：记录 producer/consumer/图访问/后备清单，首次关闭与开关往返、两视图全部帧槽和 resize 的双后端 strict 通过，重跑全 Demo 与 HPWater 同后端对照；不能靠强制开启 Pass、关闭校验、修改算法或放宽门限通过。

本轮开始状态：1.11 已通过；1.12 的 texture-view 槽位修复和 1.13 的海洋后备已落地，sampler/天空/完整 FFT 严格校验仍待实现验证。用户最新“好的”已明确批准三项继续纳入 P0，按 1.12、1.13、1.15 执行，无需再次确认。P0 门禁未通过前不进入 P1，不把普通 GPU 数值测试通过当成完整严格校验通过。

本轮结果：1.12、1.15 完成，实施进度 8/113。lights native/serial 双后端 validation 捕获通过；preview、atmosphere 原二进制同后端对照逐像素一致，HPWater underwater 原图严格对照通过。海洋/天空后备和每帧槽更新已实现，lights 配置的双视图天空开关 strict 两后端通过；但默认 deferred 双视图的 Vulkan strict 在阶段 0 暴露 SSR 绑定关闭 PlanarReflections 的未初始化半分辨率纹理（不是 SkyView）。保留失败 fixture，1.13 的完整消费者验收暂不勾选；该独立问题未静默修复，需单独纳入范围后处理。P0 全图像/时域/性能矩阵和 1.14 仍未完成。

后续授权及结果：用户“好的，请继续实现”已同意将上述 PlanarReflections 问题纳入本计划，新增 1.16。默认 deferred 双视图的两后端 native/serial 共四轮 strict（8 阶段、全部帧槽、两次 resize）通过；preview/reflections/atmosphere 两后端原图逐像素一致，HPWater 默认视角两后端严格同后端门限通过。1.13、1.16 完成，当前 10/114；继续 P0，未启动 P1。

前置修复恢复点 `20260828-p0-planar-fallback` 已创建并验证 821 文件（详见验收记录），1.14 完成，当前 11/114。全目录检查发现 Vulkan shadows 的关闭 GTAO 绑定疑点，保持 1.5–1.9 未完成；新增缺陷未授权修复，不进入 P1。

最终检查：D3D12 20/20 Demo 固定帧 capture/strict 通过（不等于完整视觉/时域/性能验收）；Vulkan 全目录前三项完成后在 shadows 失败，原始二进制同配置也失败。forward/no-Editor 补充 strict 通过。下一轮需先确认关闭 GTAO 等可选 Feature 资源绑定问题的排查修复范围，当前 11/114。

最新用户“好的”已批准统一可选 Feature 关闭状态绑定排查修复，新增 1.17，当前 11/115。同类缺陷在本范围内继续处理，无需逐个重复确认；不扩展到无关算法或绕过 P0 门禁。

本次 1.17 实施结果：GTAO 白色后备、材质 spectral/local 有效谓词、关闭中间纹理 capture 选择、OceanGpuQuery 关闭 local 的零值后备提前上传均已落地；无 shader/海洋算法/Demo 设置改动。专项严格测试 30 项、CPU 11/11+10/10、常规 GPU 16/16+8/8、工具自测 10/10 通过；D3D12 20/20 Demo 运行通过但图像 19/20，HPWater 原始二进制同后端门限两 API 通过。Vulkan 四个 Fluid/native 路径仍有 General→ShaderReadOnly 错误，指向已启用 Fluid 的共享输入图依赖/队列交接；D3D12 PBF 同一二进制三次捕获也存在超门限波动。保留失败证据，不改这两类独立问题或放宽门限；待明确纳入 P0。1.17 全目录验收未闭环，维持未勾选，整体 11/115；部分修复恢复点 `20260828-p0-optional-bindings-partial`，详见验收记录与 `docs/OPTIONAL_FEATURE_BINDINGS_AUDIT.md`。

最新用户“好的，请继续”已将以下两项明确纳入 P0，当前 11/117；不再重复请求这两项授权，原 1.17 和完整 P0 门禁仍须验收。

- [x] 1.18 修复四个 Fluid Demo 在 Vulkan native 队列下的共享输入布局/依赖错误；验证：记录同一资源所有实际消费者，增加缺失依赖回归测试，两后端 native/serial 严格校验及适用原图对照通过，不修改队列算法或以 serial 替代 native 验收。
- [x] 1.19 定位并修复 PBF 同一二进制重复捕获超门限问题；验证：固定输入/初始化/帧身份有证据，必要的确定性/同步修复保持求解方程、默认配置和步数，至少三次独立捕获满足原 V2 门限，原图兼容单独判定，不覆盖旧 golden 或放宽门限。

P0 最新实施记录：1.18 的唯一 Environment handle 和全部实际消费者读声明已实现，四个 Fluid × 双 API × native/serial 的 16 组严格校验通过。1.19 已证明初始数据一致、浮点差异从原子网格插入后的首步开始；新增桶内粒子 ID 排序后，两 API 的 GPU 数据三次重放、两 API PBF 三次独立截图及 Vulkan Toon 三次截图均逐位/逐像素一致。HPWater 默认视角原始图像两 API 对照通过，求解方程、默认参数及步数未改。PBF 双 API 与 Vulkan Toon 的旧图仍超 V2 门限，不能自行批准新基线或勾选 1.17–1.19；整体维持 11/117，未进入 P1。详见 `docs/FLUID_P0_VALIDATION.md`，候选稳定样本与旧失败报告均保留。

后续批准更新：用户最新“好的，请继续”已明确批准三组候选的新 P0 视觉参考，精确范围和 SHA 登记在 `scripts/ArchitectureVisualBaselineRevisions.json`。三组三次捕获及旧失败报告完整性复核通过，7 项基线范围/哈希/失败批次正反例通过；显式使用修订的 9 次比较全部通过。1.19 完成，当前 12/117；1.17、1.18 的最终全目录/队列复核与 1.5–1.9 仍待完成。以上历史“等待批准”不再是当前阻碍。

最终全目录复核：`P0/{d3d12,vulkan}/approved-final-all-demos-native-01/` 各 20/20 capture/strict；`P0/comparisons/approved-final-all-demos-{d3d12,vulkan}-01/` 各 20/20 原 V2 通过。仅三项使用批准参考，其余 D3D12 对上一修复版、Vulkan 对原始二进制；HPWater 两 API 均另有原始二进制对照通过。连同最终双视图/帧槽/resize 的 16+8 专项 strict，1.17 闭环，进度 13/117；完整时域、质量和性能仍需独立验收。

新增未修复阻碍：本轮把 `-Validation` 真正传入常规 GPU CTest 后，完整构建为 16/18，无 Editor 为 8/9；失败均为 OceanTessellationGpu fixture，涉及缓冲初态、呈现布局与独立测试 shader spacing。相关代码对原快照未变，生产 Demo 全目录通过不能替代 fixture 验收。详见 `docs/OCEAN_TESSELLATION_P0_BLOCKER.md`。此独立修复范围尚未批准，1.6 保持未完成，不降低校验或进入 P1。

本轮封存：最终 `approved-final-fluid-serial-01` 双 API 各 4/4 strict，连同 native 全目录中的四 Fluid 共 16/16。`approved-final-queue-diagnostic-{api}-{scene}-01` 八组当前版本 native/serial 图像逐像素一致；这是队列一致性检查，不扩大三项批准参考的 native 范围。native 旧图/批准图、资源依赖 CPU 回归及最终 CPU 11/11+10/10 均通过，1.18 完成，最终 **14/117**。工具测试 12+10 全通过；恢复点 `20260828-p0-approved-visual-baselines`。1.5–1.9 保持未完成，新的细分 fixture 修复待批准，P1–P8 未启动。

最新用户“好的，请继续”已明确批准上述独立细分测试修复，新增 1.20，当前 **14/118**。历史“待批准”已解除；只修改 fixture、独立测试 shader 及必要 CTest 属性，不改生产海洋算法或放宽数值/视觉门限。

- [x] 1.20 修复 OceanTessellation GPU fixture 的缓冲初态、呈现边界与独立 Domain shader spacing；验证：两种 Windows 构建、CPU/ShaderCompiler、三个细分 GPU 配置及完整 GPU 套件显式严格校验通过，数值断言与容差不变，生产 shader 哈希及 Demo/HPWater 图像复核，保留原失败证据及新恢复点。

1.20 修复后的可用平台基线：Windows 两构建通过，CPU 11/11+10/10，显式 `-L gpu -j1` 完整版 18/18、无 Editor 9/9；实际校验层启用的图形测试分别 17、8 项，各自另有 1 项 VulkanRuntime 单列。Linux 当前无可用环境（本机注册 WSL 发行版 0），仅登记为非阻塞兼容性状态；1.6 的 Windows 可用平台基线及环境登记完成。当前 **15/118**，1.20 尚待本轮 Demo 图像复核及恢复点，P0 整体未完成。

本轮最终闭环：两 API 的 20 Demo capture/strict 和上一基线同后端 V2 对照全部通过；HPWater 默认视角直接原始图对照通过。三个最新细分 CTest 配置实际开启校验并通过；HPWater 两 API 的既有 330 帧 lifecycle/覆盖率恢复断言也通过（不代替完整连续图像矩阵）。恢复点 `20260828-p0-tessellation-fixture` 的 833 文件创建并重复验证完成，生产 C++ 与海洋算法未改，原失败证据保留。1.20 完成，当前 **16/118**。快照后仅更新任务勾选与验收说明；1.5、1.7–1.9 及 P1–P8 保持未完成，无新增待授权的细分修复。

1.5 已完成，当前 **17/118**：新增默认关闭的帧/视图诊断、捕获帧冻结和独立同/跨后端结构比较。两 API 各 20/20 Demo capture/strict、原 V2 图像对照、结构自比较及跨 API 全目录语义检查通过；最终补齐重复完成通知、保存失败和无 Editor swapchain 接线，最终 CPU 12/12+11/11、GPU 18/18+9/9、边界集成 4+2 项以及 HPWater 两 API 各 330 帧诊断/最终图像对照通过。最终 preview/HPWater Game 与 preview Scene 共六次捕获通过，HPWater 默认视角直接原始图对照通过，工具测试 13+11 通过。完整文件/构建身份与阶段证据见 `docs/ARCHITECTURE_FRAME_DIAGNOSTICS.md`，初版全目录结果与最终边界修正二进制明确区分；实现过程失败证据保留。记录的是实际完成命令录制/回放的 Pass，不冒充 GPU dispatch 完成次数；地形既有双视图空 Pass 原样保留。1.7–1.9 的完整质量/尺寸、连续控制点图像与性能基线仍未完成，未进入 P1 或线程迁移。

1.7 完成，当前 **18/118**：新增 opt-in 同进程多帧捕获与逐请求诊断，保留旧截图路径。两 API 各三次 330 帧运行、每次 64 张控制点图像（共 384 张），全部两两图像/结构对照及 64 点跨 API 检查通过；水线、reset、resize、三海洋场景切换有图像而不再仅有最终截图。七视角、三质量×两尺寸的 26 个配置各三次，选定的 78 个样本与 78 个两两比较在 `P0/water-baseline-set-01.json` 逐项封存；两 API 首样本对既有 HPWater 图片和独立 parity 均通过。初次矩阵保留一次真实窗口被改变为 2556×1406 的错误尺寸失败；已补请求尺寸/实际尺寸校验，Vulkan 质量矩阵改用原有隐藏窗口模式重测，并验证隐藏/显示窗口等图像和图结构，未改生产窗口或算法。最终两 API 各 20/20 Demo 默认捕获/strict 与上一验收图的 V2 比较通过；CPU 12/12+11/11、GPU 18/18+9/9、三种运行路径的正常连续捕获和提前退出负例通过。详见 `docs/ARCHITECTURE_CAPTURE_SEQUENCES.md`。60 个 assets 哈希未变；1.8 的 V3 性能和 1.9 的全 Demo 连续控制/双视图/流送矩阵仍未完成，未进入 P1 或线程迁移。

1.8 本轮推进（2026-08-29），保持 **18/118**：已实现默认关闭的逐帧 CPU/进程内存/退役/设备 PSO 创建统计，以及按 view/slot/generation 关联的 GPU 计时，不新增逐帧 GPU 等待。两构建与 CPU 13/13+12/12、GPU 18/18+9/9、两 API 各 20/20 Demo 原 V2 图像、HPWater 两 API 各 330 帧/64 点连续图像和结构对照均通过。性能工具 31 项及旧工具 13+11+10 项通过，8 项真实进程配置负例通过。四负载×双 API 的两批 128 次运行全部有效，但跨批只有 D3D12 shadows、Vulkan HPWater 两项通过；另外六项超限。D3D12 preview 的 180/900/8 帧、每批 3+5 个进程的独立两批复测（16 次）仍失败：CPU frame median 7.58165→15.2265 ms，主要差异仍在 BeginFrame（1.5327→9.8226 ms）；没有证据把原因归为算法回归或计时错帧。144 次运行、1,296 个子产物封存复核通过，失败报告不覆盖；全部 60 个 assets 哈希未变。详见 `docs/ARCHITECTURE_FRAME_PERFORMANCE.md`。下一步需补齐窗口/显示器、呈现状态与帧等待细分诊断并收敛基线，不改变同步、画质或门限；1.8、1.9 仍未完成，不进入 P1 或线程迁移。

1.8 后续诊断推进（2026-08-29），仍 **18/118**：已新增默认关闭的帧槽 fence/回收/acquire/image fence/上传/提交/Present/signal 计时与原生状态、实际窗口/显示器/刷新率记录，以及性能专用的一次性坐标输入和严格正反例。普通 Demo 设置、海洋算法、Present/等待顺序未改变。两构建与 CPU 13/13+12/12、GPU 18/18+9/9、两 API 各 20/20 Demo 原 V2、HPWater 各 330 帧/64 点图像/结构比较均通过；工具 13+11+10+52、最终真实配置负例 7+7 通过，无 Editor Vulkan 性能路径通过。隐藏 D3D12 窗口实测返回 DXGI_STATUS_OCCLUDED；最初坐标在首次事件处理后恢复，最终改为首次 PollEvents 后只应用一次并验证。固定可见 (100,100)/200 Hz 的双 API preview 180/900/8 两批长测，第一批 16 次运行通过，第二批第 2 次预热在第 310 帧失去焦点而被拒绝；18 个进程中 17 个有效，没有完成第二批或形成 V3 比较结论。全部失败数据保留，161 个子产物及 index 封存复核通过。详见 `docs/ARCHITECTURE_FRAME_PACING.md`。需要焦点稳定的测试时段或显式确定新的测试窗口策略，不自动抢焦点、改同步或放宽门限；1.8/1.9 不勾选，P1/线程迁移仍未启动。

1.8 再次推进（2026-08-29），保持 **18/118**：用户确认继续后，以冻结二进制重新启动 `pacing-visible-preview-baseline-03` 双 API preview 的独立两批 180/900/8 测量。第一批全部及第二批 r1 有效；第二批 D3D12 r2 在第 682 帧 focused true→false（其他窗口/显示/正常呈现条件不变），18 个已执行进程仅 17 个有效，第二批正式测量未执行，不作 V3 通过结论。新增离线整批审计入口、公共实现和测试，26+52 项工具测试通过；新批次、旧中断批次及旧完整超限批次的真实证据重算分别核验 163/163/146 个产物，准确区分未完成与完整但超限，Enforce 按预期拒绝，未覆盖旧报告。454 项生产输入及渲染二进制哈希未变，本轮没有生产代码/HPWater/Demo 改动。详见 `docs/ARCHITECTURE_FRAME_PACING.md`。前台焦点稳定条件再次不满足；测试专用固定无焦点策略尚待明确，不自动修改桌面焦点或门限，1.8/1.9 和 P1–P8 保持未完成。

最新用户“好的”已明确批准 P0 性能测试专用固定无焦点窗口模式。仅在显式性能采样且窗口可见时允许；创建/显示不主动聚焦，采样要求实际 visible=true、focused=false，仍拒绝遮挡、异常呈现、显示条件变化，不过滤失败帧、不放宽 V3。普通窗口、HPWater、Demo、同步和画质不变，新配置单独重建两批基线，不混用旧前台数据。实现与验证完成前仍保持 1.8/1.9 未勾选、18/118。

无焦点模式本轮已实现：新增窄参数解析头、Window 策略和 Host 接线，更新性能驱动/校验/正反例；双构建、CPU 13/13+12/12、GPU 18/18+9/9、工具 13+11+10+64+26、真实负例 10+10、两 API 各 20/20 Demo 原 V2 与 HPWater 各 330 帧/64 点图像/结构全部通过。新 preview 两批 32/32 运行合法、18,000 个正式帧；Vulkan 通过，D3D12 仅 Scene GPU 超限（median 1.13646→0.84365 ms，反向 +34.708%；p95 反向 +50.699%）。离线重算核验 292 个产物，complete=true/passed=false；455 项生产输入及二进制未在测量中变化，60 个 assets 不变。无焦点模式解决本轮采样条件连续性，但不证明整体性能稳定；按 openspec-apply-change 暂停在该 GPU 分布阻碍，不删样本、不改同步/电源/门限、不进入 P1。其余负载新协议基线和全 Demo 连续矩阵仍待完成，**18/118**、1.8/1.9 保持未勾选，详见 `docs/ARCHITECTURE_FRAME_PACING.md`。

用户再次确认继续后，已完成 D3D12 Scene GPU 的只读 vendor telemetry 专项：新增旁路测量/common/离线汇总和测试，不改生产源码、GPU 状态或 V3。工具 8/8、最终版 2 次真实覆盖自检有效；主诊断 8 次保留，离线只纳入 5 次同时覆盖初始化到退出且原性能有效的进程，Scene GPU/graphics clock Pearson **-0.99053**，Game GPU 0.34474、CPU frame 0.10665。结果强烈支持 DVFS 相关但不证明唯一因果，不能自行豁免。锁频/reset 虽由驱动提供但属于未授权系统状态改变；更多运行/外部条件筛选也会改变协议。保持 **18/118**、1.8/1.9 未勾选并按 openspec-apply-change 暂停，待用户在临时锁频并可靠恢复（推荐）、明确修订更大样本协议、或明确接受本机 DVFS 例外之间选择；P1/线程迁移未启动。

用户已批准临时锁频并要求核对 FPS/Unity/UE 口径。本轮新增实际循环 FPS 对照和带 `finally`/三次 reset 的 opt-in NVIDIA 包装器，不改生产渲染代码。封存样本显示同一 Vulkan preview 严格校验下，完整 Editor `game,scene` 中位 166.872 FPS，无 Editor `game` 中位 363.458 FPS；当前 P0 双视图计时确实包含实际 Scene Render，`-View game` 仅选择捕获。Unity/UE 官方资料支持将总帧/循环、主线程、渲染线程、GPU 分开报告，不能把阶段相加或倒数单个 GPU Pass。2400 MHz 请求被驱动以当前用户无权限（exit 4）拒绝，Benchmark 未启动、锁定从未成功，reset 同样被拒；只读状态未显示 applications-clock limiter。失败证据保留，不冒充锁频基线。工具 2+8+64 及真实 view-throughput 汇总通过，但管理员条件缺失仍阻止该因果实验；保持 **18/118**、1.8/1.9 未勾选，不进入 P1。

本轮恢复点 `20260829-p0-fps-scope-clock-permission`：882 文件，创建及重复校验通过，manifest SHA `739DA9364EBEFEA987CB9B3EB7487AA2B69A576D31FAAC2115F670C97F2085A7`。恢复点包含新增四个脚本/测试、FPS/权限记录和未勾选任务状态；封存后只追加该哈希说明。

用户要求继续并明确定位 100 多 FPS 的耗时后，本轮在 1.8 内新增默认关闭、仅性能采样可用的同二进制 `game` active-view 隔离。Vulkan preview strict 的完整 Editor A/B：Game+Scene loop median 5.7432 ms / 174.119 FPS，Game-only 3.25615 ms / 307.111 FPS；Scene Render 差 1.81085 ms、BeginFrame 差 0.45615 ms、Game Render 仅差 0.0372 ms。DeferredLighting/SSR/TAA 等具体 GPU Pass 中位为 0.06/0.05/0.04 ms且双视图前后基本不变，已把主要成本定位为第二个 Scene view 与伴随的帧/队列压力，不把外层 GPU Renderer 区间误称为单一延迟 Pass。报告 SHA `02C21A5E0E7A9062D95FA484CA10D85F4F9A9A83301D3F907BE81E0D4D179756`。完整构建、C++ 测试、性能工具 65/65、Vulkan 真实负例 13/13、普通路径双 API 各 20/20 Demo strict/原 V2（含 HPWater）通过；D3D12 A/B 因 `DXGI_STATUS_OCCLUDED` 被门禁拒绝并保留。该诊断不替代两批 V3，1.8/1.9 仍未勾选，保持 **18/118**，不进入 P1/线程迁移。

本轮恢复点 `20260829-p0-active-view-cost`：884 文件，创建及重复校验通过，manifest SHA `DF79D214951A8E2AFE5343E9C38B88446911EA9B8D6C59DBE79D34B1FE21F73B`；封存后仅追加该哈希说明。最终两构建、CPU 13/13+12/12 与 OpenSpec strict 通过。

1.9 已完成，当前 **19/118**：新增严格 20 Demo 连续目录、version 2 有序控制动作和双后端重复矩阵。D3D12/Vulkan 各 20 scenes、40 runs、20 comparisons 全部通过；三个 Ocean、四个 Fluid、TAA/阴影开关、流式激活和 WaveWorks 双视图均有连续采样及结构断言。HPWater 沿用原 330 帧 lifecycle，采样 301 帧而非合法的近均匀第 300 帧 debug buffer；生产海洋算法、shader、Demo 默认值、同步和普通双视图策略未改。实现失败证据保留，完整文件/数据流/索引 SHA 见 `docs/ARCHITECTURE_DEMO_SEQUENCES.md`。两构建、脚本正反例及 C++ 测试通过；CTest 初跑的两个 Vulkan layer-path 失败在项目规定 `VK_LAYER_PATH` 下保持校验复测 2/2 通过。1.8 仍未完成，因此不进入 P1 或线程迁移。

本轮恢复点 `20260829-p0-demo-sequences`：888 文件，创建及重复校验通过，manifest SHA `57CE3BE2BD69A1830F25B80CADBB42694B102D7E7E36B811C2FB04CC96B9E441`；封存后仅追加该哈希说明。

管理员环境下的 1.8 继续验证（2026-08-29），仍为 **19/118**：性能窗口现以显式策略恢复最小化状态、固定尺寸/位置，并在无焦点模式使用 `WS_EX_NOACTIVATE`、topmost 与原前台窗口回交，最终 D3D12 长进程稳健性矩阵 **8/8** 满足 visible=true、focused=false、1280×800、非最小化、Present 成功且 Game/Scene GPU 计时完整。锁频包装器增加实际 graphics clock 收敛轮询；本机 2400 MHz 请求成功并收敛至 2392 MHz，失败退出后已执行 `-rgc`，最终只读状态为动态 2812 MHz、limiter Not Active。完整锁频基线的 D3D12 第一批四负载 **32/32** 完成；进入 Vulkan 时，管理员完整性级别下的 Vulkan Loader 忽略项目 `VK_LAYER_PATH`，因缺少 `VK_LAYER_KHRONOS_validation` 以 `VK_ERROR_LAYER_NOT_PRESENT` 退出。未关闭严格校验、未混用 D3D12 样本、未写 HKLM Vulkan 注册表；GPU 已恢复，HKCU/HKLM 临时测试键均不存在。C++ 测试、GPU clock 工具 **3/3**、性能工具 **72/72** 和 OpenSpec strict 通过。要继续完整 V3，需用户明确批准在运行期临时注册机器级 Vulkan ExplicitLayers 值并接受异常中断可能残留的系统范围风险；批准前 1.8 保持未勾选，不进入 P1。

用户已否决继续把管理员锁频与机器级 Vulkan layer 注册作为架构前置门禁。上述实验和失败证据保留，但“需写 HKLM 才能进入 P1”的历史结论由修订后的 proposal/design/tasks 取代；后续不得执行该注册方案。P0 先交付实时可观测性和公平场景矩阵，再按普通 strict/图像行为与 matched A/B 关闭。

- [x] 1.21 新增 `FrameProfilerSnapshot`、`FrameProfiler` 和 CPU 测试，定义 240 帧预分配 ring、完成帧身份、`off/basic/detailed/capture` 等级、Main/Render/Worker/GPU/Wait/View 字段及 unavailable 语义；验证：容量回绕、跨线程提交、旧 GPU generation、缺失 lane、重复完成和停机测试通过，无无界容器。
- [x] 1.22 将当前单线程主循环的事件/UI/场景发布/Game/Scene/RenderGraph/BeginFrame/Submit/Present/等待接入 FrameProfiler，记录 thread id、active views、Scene 刷新原因和 resolved GPU frame；验证：inline Render 明确为 Main 子区间，Game/Scene 不错配当前 CPU 帧，不增加 `WaitForGpu`，现有离线 recorder 字段保持兼容。
- [x] 1.23 为 `GpuProfiler`、RenderGraph 诊断和 frame pacing 接入分级采样；验证：默认 basic 仅保留粗粒度总量和等待，detailed/capture 才启用逐 Pass/queue 详情，off 不写可选 timestamp；运行时切换只影响后续帧，四等级正反例和 off/basic/detailed 同进程开销报告通过。
- [x] 1.24 新增 `PerformanceProfilerPanel` 并接入 Editor/Viewport overlay；验证：显示 Editor Loop FPS、Main/Render/Worker/GPU、Game/Scene、Submit/Present/Wait、active-view/profiling level 和最近 240 帧时间线，原模糊 `FPS` 改为明确范围，面板只读已完成快照且折叠时不构建详细表。
- [x] 1.25 增加 session-local Scene View `catalog-default/live/on-interaction/30hz/paused` 诊断策略；验证：默认逐 Demo 行为和全部既有图像不变，策略切换不停止 Game/共享模拟，显式 Scene capture 强制正确刷新，当前策略与实际刷新帧进入 profiler/capture 报告。
- [x] 1.26 新增 `ArchitecturePerformanceScenarios.json` 与 `Benchmark-ArchitectureScenarios.ps1`，构造不进入 DemoSceneCatalog 的空负载并运行完整 preview 的 Editor 双视图、同 EXE Game-only、Standalone Game-only matched 矩阵；验证：尺寸/backend/build/validation/profiling level 不匹配时拒绝比较，输出实际 loop FPS 与各 lane/view/等待分布，不倒数单 Pass 或把阶段求和。
- [x] 1.27 增加 FrameProfiler/Panel/等级/Scene 策略的 C++ 与脚本正反例，并运行普通用户权限的 D3D12/Vulkan strict、Release/Standalone 冒烟；验证：Profiler off/basic/detailed 不改变固定输入图结构、模拟次数和图像，详细模式开销单列，不要求 GPU 锁频或 HKLM layer 注册。
- [x] 1.28 对 20 Demo、HPWater 连续帧和三个 Ocean/四个 Fluid 的默认 `catalog-default + basic` 路径执行双后端回归；验证：画质、时间推进、双视图、reset/resize/scene switch 与已封存基线一致，任何默认行为回归阻止 P0 完成，Scene 限频/暂停结果不用于放行。
- [x] 1.29 汇总实时 Profiler 与公平场景矩阵，完成修订后的 P0 检查点并保存恢复点；验证：1.8 和 1.21–1.28 均有可重放证据，普通 strict/图像/连续帧通过，报告明确当前 CPU/GPU/等待瓶颈及观察开销后才进入 P1，不再以管理员锁频、机器级 Vulkan 注册或不可控 DVFS 单独阻塞。

跨 change 条件：`fix-waveworks-ocean-controls-spectrum-and-foam` 当前 36/38，`add-ocean-migration-stage-visibility` 的 12/80 前提已过期。P0–P2 可在没有共同写入时推进；进入 P3 的 Ocean/WaterOptics 适配前，必须先收尾或明确暂停 WaveWorks 剩余验证，并更新、暂停或归档过期 migration visibility 计划，不能按旧进度实现 UI。

1.25/1.28 最终回归（2026-08-29）：Scene 策略控制器、Profiler 报告和显式 Scene capture 的单元/行为测试通过；默认 `catalog-default + basic` 使用一致重建后的 `build-windows-ci/RelWithDebInfo/PrismRender.exe` 运行。D3D12、Vulkan 均完成 **20 scenes / 40 runs / 20 repeat comparisons**，并各自再与任务 1.9 的冻结矩阵执行 **20/20** 同后端图像/结构比较；HPWater 两 API 均包含两次 330 帧 lifecycle，Scene View、reset/resize/scene switch、共享模拟及历史状态一致。证据为 `p0-default-basic-demo-sequences-{d3d12,vulkan}-20260829-03` 与 `p0-default-basic-vs-frozen-{d3d12,vulkan}-20260829-02`。初次候选矩阵暴露验证二进制中的 `WaterValidationSequence.cpp.obj` 过期，导致 HPWater 首帧后缺 Scene View；同源码的完整重建探针每帧均为 `requiresRefresh=1`，强制该翻译单元重编译后双后端全部恢复，失败批次保留且不用于放行。生产海洋算法、shader、Demo 默认值未修改；仅为已有生命周期刷新赋值补充契约注释。

1.8 最终公平特征基线（2026-08-29）：当前源码完整重建 Release Editor 与无 Editor Vulkan 二进制，使用 1280×800、strict validation、serial、basic、30 warmup + 120 samples + 8 drain，运行三轮独立的六场景矩阵，共 **18/18** 进程。聚合报告 `p0-performance-scenario-aggregate-vulkan-release-20260829/report.json` 严格核对 build/backend/validation/level/view/尺寸/adapter/executable。跨三轮中位：空负载 Editor 双视图 **373.41 FPS**、同 EXE Game-only **561.32 FPS**、Standalone **977.04 FPS**；完整 Preview 分别 **260.75 / 414.63 / 556.14 FPS**。Preview 双视图的 loop/main-active/main-wait/Scene CPU/GPU hint 为 **3.80/2.94/0.61/0.99/0.35 ms**；Render 仍是 Main 子区间，Worker unavailable，不能相加。另有同一 Release 进程相邻 240 帧 `off/basic/detailed/capture` A/B（每级 60 帧、去 10 帧预热），Profiler 自身中位 **0.0001–0.0002 ms**；RelWithDebInfo detailed sidecar 记录工作集、commit、retirement 和测量期 PSO 创建为 0，明确不与 Release FPS 直接比较。聚合器正反例通过；DVFS/显示只作条件，不再要求机器级改动。

P0 检查点已完成：两套 Release 构建成功，相关 CTest 为 Editor **5/5**、无 Editor **4/4**，OpenSpec strict 通过；默认双后端全 Demo/HPWater 冻结比较、Profiler 等级行为、公平场景及详细 sidecar 均可重放。恢复点 `20260829-p0-profiler-fair-matrix` 共 **913** 文件，创建和二次验证通过，manifest SHA-256 `B1025EE93B99E17BDABD0F378323A91B119B4789559976F32EB7ABA58C07EA3F`。恢复只允许复制至新的空目录，不覆盖当前工作区或旧证据。当前 **29/128**，P0 全部任务完成，可以进入 P1；尚未开始 Host 职责迁移或线程化。

## 2. P1 — 提取 ApplicationHost 的实际职责与所有权


- [x] 2.1 新增 `tests/ApplicationCompositionTests.cpp` 和最小可测试动作接口，固定初始化、帧阶段及退出顺序；验证：当前实现的正常、无 Editor、提前退出和部分初始化失败路径有可比对事件轨迹，不引入渲染算法变化。
- [x] 2.2 新增 `AssetRuntimeCoordinator`，迁移 ImportService/Registry/StreamingManager 所有权及导入/上传/驻留协调；验证：导入、上传完成、取消和停机 join 测试通过，GPU 上传仍发生在原渲染准备安全点，worker 不直接修改帧快照。
- [x] 2.3 新增 `SceneSession` 和 `SceneSessionActions`，迁移 CommandProcessor/World、editable RenderScene、相机、场景标识与 bridge；验证：加载/保存、Demo 切换、undo/redo/journal 和现有 World bridge 测试通过，Session 不反向依赖 Host 或 ImGui。
- [x] 2.4 新增 `RenderFrameCoordinator`，迁移双 SceneRenderer 与 SharedResources 所有权，暂保留现有帧输入；验证：Game/Editor 渲染顺序、共享模拟次数、隐藏/按需刷新和 resize 轨迹与 P0 一致，不改变队列模式或生产策略。
- [x] 2.5 新增 `EditorCoordinator`，迁移 EditorLayer/ImGuiSystem/panels 的会话、动作和统计组装；验证：选择、编辑、撤销、文件对话框、面板和两相机操作冒烟通过，无 Editor 构建不引用 ImGui，未引入 Host 指针或 friend 访问。
- [x] 2.6 新增 `CaptureAutomationController`，迁移延迟截图、流送就绪判断、报告和退出状态；验证：现有 CLI 测试通过，固定帧捕获、max frames、失败退出码及已有报告字段与 P0 一致。
- [x] 2.7 将 Host 的水验证成员方法迁入独立 `Core/Application/WaterValidationSequence` 并移除旧定义；验证：HPWater lifecycle/views/details 的动作顺序、相机/质量设置和输出文件契约不变，旧成员定义及重复状态已无调用者。
- [x] 2.8 收敛 Host 为组合根和主循环，明确 IO/GPU/UI/视图/资产/后端的 RAII 销毁顺序；验证：逐阶段初始化故障注入和重复 Shutdown 测试通过，不访问未创建对象，不提前销毁在途资源，正常退出不重复全局 GPU 等待，main 保持最小入口。
- [x] 2.9 执行 P1 集成验收并保存服务级恢复点；验证：现有 CPU suites、全部 Demo 冒烟、HPWater 同后端图像/生命周期对照及 V3 性能门限通过，Host 不再持有业务状态机和 UI 统计算法后进入 P2。

2.4 已完成，当前 **33/128**：新增 `RenderFrameCoordinator`，成为 Game/Scene 两个 `SceneRenderer` 与 `SceneRendererSharedResources` 的唯一 owner；Host 仅保留由 Coordinator 生命周期覆盖的非拥有热路径别名。初始化、诊断等级、profiling、scene-changed、双 renderer resize 与幂等销毁均通过 Coordinator，当前帧输入和 inline 执行位置不变，没有引入渲染线程、RHI 线程或新队列策略。专项测试固定 Game→Scene 顺序、隐藏/按需 Scene 计划和唯一共享模拟 producer；完整 Editor/无 Editor 构建通过，相关测试两配置各 3/3。D3D12/Vulkan 完整 Editor 各完成 330 帧 HPWater lifecycle 和 64 点同后端图像/结构比较，resize、reset、scene switch 与 2.3 基线一致；无 Editor Vulkan 同序列 64 点始终为 Game-only 且直接输出 swapchain。GPU 集按项目 validation layer 环境为 18/18+9/9；生产算法、shader、Demo 默认值及 queue mode 未改。下一项是 2.5 `EditorCoordinator`。

2.5 已完成，当前 **34/128**：新增 UI 所有的 `EditorCoordinator`，独占 `ImGuiSystem`、`EditorLayer`、Debug/Profiler panels，并迁移文档/命令动作适配、只读统计组装、双 viewport 纹理注册及两相机输入路由。Coordinator 只消费 `SceneSession`、资产 registry、renderer/profiler 和窄回调，不保存或 friend `ApplicationHost`；Host 只组装逐帧输入并消费 requested Demo/scene-dirty 结果。专项测试覆盖 Game/Scene/gizmo 导航选择、文件对话框链接边界、SceneSession save/load、事务 edit/undo/redo、dirty 回调和重复 UI session Shutdown；完整 Editor 非 GPU 为 22/23，唯一失败仍是 CTest 的 Windows PowerShell 5 兼容项且 PowerShell 7 直跑通过，无 Editor 为 21/21。D3D12、完整 Editor Vulkan、无 Editor Vulkan 各完成 330 帧/64 点 HPWater lifecycle，对 2.4 同配置图像和结构均 64/64 通过；无 Editor build graph 不含 PrismEditor/ImGui/EditorCoordinator。下一项是 2.6 `CaptureAutomationController`。

2.6 已完成，当前 **35/128**：新增 `Core/Application/CaptureAutomationController`，集中拥有 `FrameDiagnostics`、`FrameCaptureSequence`、延迟/deferred capture、流送激活结果、RDG/GPU/streaming 报告路径与写出状态、max frames 和退出条件；Host 不再保存这些状态或实现截图请求辅助方法。控制器只借用 backend、renderer、SceneSession 和 AssetRuntimeCoordinator 完成原同步操作，不保存 Host 指针，不移动 BeginFrame、上传、readback、Present 或退出检查位置。CLI/诊断/Coordinator 契约测试两配置通过；完整非 GPU 为 22/23，唯一失败仍是 CTest 选择 Windows PowerShell 5，同脚本 PowerShell 7 通过，无 Editor 为 21/21；GPU 为 16/18 后按项目 validation layer 路径复测缺失的 Vulkan 2/2，通过总集 18/18，无 Editor Vulkan 9/9。D3D12、完整 Editor Vulkan、无 Editor Vulkan 各完成 330 帧/64 点 HPWater lifecycle，对 2.5 同配置图像和结构分别 64/64 通过。D3D12/Vulkan 的 streaming 激活序列各 7/7 通过，Scene report 保持 v1、20 个对象；固定 `max frames=4` 的未完成序列在两后端都以非零退出且只保留前两个成功 capture。RenderGraph v11、GPU timing v2 的字段集合与 P0 零差异，Capture diagnostics 保留全部 P0 字段。生产算法、shader、资产、Demo 默认值、线程和 queue mode 未改。下一项是 2.7 `WaterValidationSequence`。

2.7 已完成，当前 **36/128**：原 `src/Core/WaterValidationSequence.cpp` 虽已物理分文件但仍定义 Host 成员；现已替换为真正独立的 `Core/Application/WaterValidationSequence.{h,cpp}`，由 CaptureAutomationController 独占。它缓存 opt-in 状态，只接收当前 frame、renderer、editable scene、window 和单一 Demo 激活回调，返回 Scene View 是否需要刷新，不保存 Host、backend 或跨帧资源。Host 的旧声明、定义和调用检索为零。完整 Editor/无 Editor 构建及相关契约测试 7/7、6/6 通过；D3D12、完整 Editor Vulkan、无 Editor Vulkan 各重放 330 帧/64 点 HPWater lifecycle，对 2.6 同配置图像和结构均 64/64 通过，29 条 WaterValidation progress 事件逐条相同。resize、reset、相机/水线、三质量、caustics、debug views、WaveWorks→Ocean→HPWater 切换和输出文件契约不变。下一项是 2.8 Host 组合根与 RAII 销毁顺序收敛。

2.8 已完成，当前 **37/128**：ApplicationHost 以 `Uninitialized/Initializing/Initialized/ShuttingDown` 显式状态代替末尾才置位的布尔 guard；Initialize 任一阶段失败都立即执行只针对已创建资源的幂等 Shutdown，析构再次调用不会重复清理。backend 成功后、GPU 尚未 drain 时才等待；正常 Run 尾部已 drain 后析构不再重复全局 WaitForGpu，只有尾部 streaming pump 确实提交新上传时恢复必要等待。新增可重放 `ApplicationLifecycleIntegrationTests.ps1`，D3D12 与 Vulkan/no-editor 的 normal + services/backend/content/renderers/editor **6/6 + 6/6** 通过；services 不访问未初始化 backend，其余故障各等待一次，所有进程恰有一次 shutdown begin/completed。ApplicationComposition 正常/早退/逐阶段初始化/运行异常契约通过。三配置 HPWater 330 帧/64 点对 2.7 均 64/64，D3D12 streaming 7/7 且无尾部新上传时 `waitForGpu=false`。`main.cpp` 未改。下一项是 2.9 P1 集成验收与恢复点。

2.9 已完成，当前 **38/128**：完整/no-editor 非 GPU 为 22/23（唯一 Windows PowerShell 5 脚本兼容项由 pwsh 7 直跑通过）与 21/21，显式 validation 的 GPU 为 18/18+9/9；完整 Editor 的 D3D12/Vulkan 各 20/20 Demo 默认 native 固定帧 capture/strict 通过。HPWater 两后端各连续 330 帧、64 控制点，相对 2.8 同后端图像与结构均 64/64。V3 Vulkan preview/native 两批 3+5、每次 180 帧的首次采样因 Scene GPU 中位 +8.34% 保留失败，等协议复测的 CPU frame、Editor Loop、Game/Scene GPU 双向中位/p95 全部通过 5%/10% 门限。Host 不拥有 CommandProcessor/World、资产、Renderer、Editor/UI 统计或 capture/water 状态机，服务无 Host 反向指针；仅保留组合、主循环和带 owner 保活的非拥有热路径别名。服务级恢复点 `20260830-p1-service-boundaries` 已逐文件 seal 验证；P1 完成，下一项是 3.1 RenderGraph compiler/hazard 基线测试。

## 3. P2 — 保持 RenderGraph 公共 API，拆分内部编译与执行

- [x] 3.1 新增 `RenderGraphCompilerTests.cpp`，在拆分前固化输出裁剪、资源版本、history、别名和跨队列案例；验证：基线通过，包含传递约简不能丢资源释放、首 compute prologue 以及逐 mip 恢复的回归断言。
- [x] 3.2 引入 `GraphDescription`、`CompiledGraph` 和 `GraphExecutionState` 数据边界，RenderGraph 继续提供原 API；验证：所有现有调用方无需改变建图语义即可编译，测试证明编译计划不保存前帧可变执行状态或悬空回调。
- [x] 3.3 提取 `GraphCompiler` 的有效性检查、liveness、Pass 裁剪及依赖构建；验证：CPU fixture 对照保留 Pass、完整资源访问关系和约简执行 DAG，公开诊断字段与裁剪结果不变。
- [x] 3.4 提取 `ResourceLifetimePlanner`；验证：首末访问、导入/导出资源、history、子资源和无消费者资源的寿命区间与基线一致，不依赖整个 RenderGraph 私有状态。
- [x] 3.5 提取 `TransientAliasPlanner`，保留现有兼容性、池复用与 alias 依赖规则；验证：可重用/不可重用资源、别名屏障和跨队列复用 fixture 通过，别名依赖未被后续排序或约简丢弃。
- [x] 3.6 提取 `QueueScheduler`，保持成本模型、serial/native/auto 选择和批次规则；验证：固定输入的 queue batches/fence 计划与基线一致，队列释放依据完整访问关系，成本模型 cache 行为未改变。
- [x] 3.7 提取 `ResourceStateTracker` 及 barrier/handoff 规划；验证：逐 mip/layer、buffer range、UAV/alias、graphics prologue、跨帧导入状态测试通过，upload/readback 固定状态和相同原生状态不产生非法 transition。
- [x] 3.8 提取 `GraphExecutor`，接入每次执行独立状态和当前资源绑定；验证：serial/native/parallel recording 轨迹与等待规则通过，录制失败路径安全，Vulkan 呈现和 D3D12 context 的既有同步修复不改动。
- [x] 3.9 接回编译缓存、诊断与计时报告，清理 facade 内已迁出的状态；验证：cache hit 使用当前资源/初始状态，描述或影响计划的输入变化正确失效，旧计划不引用旧图 lambda，现有 RenderGraph tests 与报告解析通过。
- [x] 3.10 执行 P2 风险抽样集成验收；验证：关键 hazard GPU fixtures、HPWater native lifecycle、serial/auto 冒烟、代表性同后端图像和单批性能诊断完成，阶段恢复点与公共 API 保持后进入 P3；不重复全 Demo/双批 V3，四个粒子流体 Demo 排除。

3.1 已完成，当前 **39/128**：新增独立 `RenderGraphCompilerTests` CPU 契约目标，在生产拆分前固定 output reachability/pass culling 与约简依赖、texture version/generation/history、transient lifetime/alias dependency、native queue handoff，以及 imported/reused transient 的首 compute prologue 和单 mip UAV 写后恢复。测试使用最小 mock RHI，不修改 RenderGraph 公共 API、生产执行代码、队列策略或渲染功能。完整 Editor 与 no-editor Vulkan 配置中的既有 `RenderGraph` 和新增 `RenderGraphCompiler` 均为 **2/2** 通过。下一项是 3.2：建立 `GraphDescription`、`CompiledGraph` 与 `GraphExecutionState` 内部数据边界。

3.2 已完成，当前 **40/128**：新增 `Renderer/Graph/GraphDescription.h`、`CompiledGraph.h`、`GraphExecutionState.h`。声明/回调、无回调可复制编译计划、当前纹理/缓冲状态与逐次执行计时现由三个 owner 分别持有；RenderGraph 保留原嵌套类型名称和全部公开方法，通过类型别名及迁移期私有引用维持源码兼容，并显式禁止复制/移动以避免 owner 引用重绑。编译 cache 直接使用无回调 `CompiledGraph`；执行开始清空上一帧计时，成功后把执行诊断保存在 `GraphExecutionState`，并恢复纯编译摘要，失败执行不发布半帧摘要。测试销毁带捕获对象的 `GraphDescription` 后确认 plan 不延长回调生命周期，并确认执行摘要复位不污染 plan。完整 Editor/no-editor Vulkan 的全部目标均编译链接，新旧 RenderGraph 测试各 **2/2**；非 GPU 为既有 PowerShell 5 单项兼容失败且 pwsh 7 通过，以及 no-editor **22/22**。下一项是 3.3 提取 `GraphCompiler` 的校验、liveness、裁剪与依赖构建。

3.3 已完成，当前 **41/128**：新增 `Renderer/Graph/GraphCompiler.{h,cpp}`，从 RenderGraph facade 移出 declaration index 校验、liveness dependencies、output/side-effect 裁剪、稳定 execution index、RAW/WAR/WAW/跨队列与同队列依赖构建、传递约简及无回调 pass plan 写出。Facade 的旧四个成员实现和声明已删除，`Compile()` 仅按原顺序编排独立 compiler 与后续 planner；生命周期、alias、queue batch、barrier 和 executor 尚未移动。直接 CPU fixture 固定四 Pass/一死分支，确认 3 个 retained Pass、完整 A/B/Output 访问及 Consumer 的约简依赖 `{2}`，并验证非法 declaration index 被拒绝；既有 output culling/版本/history/alias/handoff fixture 继续通过。完整 Editor 与 no-editor Vulkan 全部目标编译链接，新旧 RenderGraph 测试各 **2/2**。下一项是 3.4 `ResourceLifetimePlanner`。

3.4 已完成，当前 **42/128**：新增 `Renderer/Graph/ResourceLifetimePlanner.{h,cpp}`，只读取 `GraphDescription` 与 `GraphExecutionState`，向 `CompiledGraph` 写入资源类型、初态、导入/history/version、首末访问、活动性、估算字节和原生 transient allocation 元数据；旧 `RenderGraph::BuildResourceLifetimes` 及其私有纹理字节估算实现已删除。直接 fixture 覆盖 imported Input、4 mip transient、Output、History version 2 和被裁剪的无消费者资源，确认区间 0..0、0..1、1..1、unused invalid 及 mip 2 声明不被 planner 改写；既有 alias/history/subresource 测试保持。完整 Editor/no-editor Vulkan 全部目标编译链接，新旧 RenderGraph 测试各 **2/2**。下一项是 3.5 `TransientAliasPlanner`。

3.5 已完成，当前 **43/128**：新增 `Renderer/Graph/TransientAliasPlanner.{h,cpp}`，从 facade 移出纹理/缓冲兼容性、逻辑 alias slot、原生 transient pool 统计/寿命校验及 alias 依赖注入；Planner 只接收 `GraphDescription`、`GraphExecutionState` 与 `CompiledGraph`，旧三个 RenderGraph 成员声明/定义和专用兼容 helper 已删除。直接 fixture 同时固定兼容非重叠复用、同描述寿命重叠不复用、描述不兼容不复用、native 同池/同 allocation 统计及 graphics/compute 之间的 alias 依赖；传递约简后新消费者仍只依赖前一资源最后使用 Pass。既有 facade fixture 的实际 texture/buffer alias barrier、native pool 和执行计数保持通过。完整 Editor/no-editor Vulkan 全部目标编译链接，两配置新旧 RenderGraph 测试各 **2/2**，非 GPU 回归分别 **24/24** 与 **22/22**。没有改变 RenderGraph 公共 API、queue scheduler、barrier/executor、HPWater/Ocean/Fluid、shader、Demo 默认值、线程或 Present。下一项是 3.6 `QueueScheduler`。

3.6 已完成，当前 **44/128**：新增 `Renderer/Graph/QueueScheduler.{h,cpp}`，只读取约简后 Pass DAG，向 `CompiledGraph` 写入跨队列资源同步、queue batch、Pass→batch 映射、稳定拓扑提交顺序及重叠统计；旧 `BuildQueueSchedule`/`BuildQueueBatches` 成员声明与定义已删除。直接 fixture 固定 Root→Graphics/Compute branches→Join 的 4 batch 计划、映射 `{0,1,3,2}`、提交顺序 `{0,3,1,2}`、2 条跨队列依赖及同步资源 `ComputeInput`/`ComputeResult`，并确认 planner 不改 automatic/native 决策输入。既有 facade fixture 继续覆盖 serial、显式 native、无历史 automatic、盈利 automatic、独立 batch fence/wait/graphics resume 和 QueueSchedulingCostModel 的持久 cache/预测行为。完整 Editor/no-editor Vulkan 全部目标编译链接，两配置新旧 RenderGraph 测试各 **2/2**，非 GPU 回归分别 **24/24** 与 **22/22**。queue cost model、执行选择算法、barrier/executor、HPWater/Ocean/Fluid、shader、Demo、线程与 Present 未改。下一项是 3.7 `ResourceStateTracker`。

3.7 已完成，当前 **45/128**：新增 `Renderer/Graph/ResourceStateTracker.{h,cpp}`，集中拥有冲突访问校验、逐 mip/layer texture 状态、buffer range 分裂/合并、UAV 与 native alias barrier、资源转 Common、顺序 queue handoff 及 batch tail handoff；旧七个 RenderGraph barrier/handoff 成员声明和实现已删除。执行器仍在原位置按原顺序调用，batch release 继续从完整连续访问关系推导，不依赖可能被约简掉的直接边。直接 fixture 验证同态 ShaderResource 不发 transition、指定 mip 2/layer 1 的 UAV transition/同态 UAV barrier/恢复、graphics→compute 完整资源转 Common，以及 native alias barrier 的 before/after 身份、状态清空和计数。既有 facade fixture 继续覆盖 buffer range、全资源后备、首 compute graphics prologue、跨帧导入恢复和 queue handoff。完整 Editor/no-editor Vulkan 全部目标编译链接，两配置新旧 RenderGraph 测试各 **2/2**，非 GPU 回归分别 **24/24** 与 **22/22**。RHI barrier 规则、queue/executor、HPWater/Ocean/Fluid、shader、Demo、线程和 Present 未改。下一项是 3.8 `GraphExecutor`。

3.8 已完成，当前 **46/128**：新增 `Renderer/Graph/GraphExecutor.{h,cpp}`，独立执行无 context、serial/native switching、independent queue batches 与现有 `std::async` parallel recording；它只接收 `GraphDescription`、当前 `GraphExecutionState`、`CompiledGraph`、profiling 开关和本次 command context/marker。RenderGraph 两个公开 `Execute` 保持签名，只先 `Compile()` 再转交 executor；旧 facade 的 ExecuteQueueBatches、ValidateReads、RecordPassResult 和两套执行实现已删除。RAII 执行 scope 迁入 executor，失败不发布部分 timing/summary。直接 fixture 验证同步 Pass 异常后状态退出且下一次可恢复，并以 graphics→失败 parallel compute recording→graphics 的 native DAG 证明 future 异常在任何 BeginQueueBatch 前传播、无部分 GPU 提交。既有 facade fixture 的 serial/native/automatic、batch fence/wait、graphics prologue、native/deferred parallel recording 与状态恢复保持通过。两配置全目标编译链接，新旧 RenderGraph 各 **2/2**，最终新增失败 fixture 两配置均通过；非 GPU 回归在生产迁移后分别 **24/24** 与 **22/22**。Vulkan/D3D12 context、RHI submit/Present、HPWater/Ocean/Fluid、shader、Demo、线程与 queue 算法未改。下一项是 3.9 cache/diagnostics 接回与 facade 临时状态清理。

3.9 已完成，当前 **47/128**：编译缓存继续只保存无回调、无 live RHI 指针的 `CompiledGraph`，cache hit 将计划复制到当前 compiled owner，但执行资源、初始状态和回调始终来自本轮 `GraphExecutionState` / `GraphDescription`。移除 facade 中全部 compiled/execution 引用别名，diagnostics、计时和 queue profile 直接读取明确 owner；`Reset()` 改为调用 `CompiledGraph::Clear()` 与 `GraphExecutionState::InvalidateCompiledPlan()`，不再短暂暴露旧 passes、fingerprint、签名、资源或已完成摘要。扩展 RenderGraph fixture：相同拓扑换用新纹理与新 lambda 后命中 cache，barrier/ResolveTexture/报告均指向当前资源且旧 lambda 不执行；初始状态或 texture description 改变均 cache miss；Reset 后编译、资源、签名和 timing 诊断为空。完整 Editor/no-editor 两配置全部目标编译链接，RenderGraph/Compiler 两配置各通过，非 GPU 回归分别 **24/24** 与 **22/22**。queue scheduler、barrier、RHI submit/Present、线程、HPWater/Ocean/Fluid、shader 与 Demo 未改。下一项是 3.10 P2 图等价与双后端集成验收。

3.10 已按用户确认的精简策略完成，当前 **48/128**：完整/no-editor GPU hazard fixtures 为 **18/18 + 9/9**；HPWater D3D12/Vulkan native 330 帧 lifecycle 均执行成功并通过既有同后端最终图像门限，serial/auto 各两后端 60 帧冒烟完成，报告保留 requested/actual queue mode。策略调整前已完成的 D3D12/Vulkan 各 20 场景候选和同后端比较均为 **20/20**，结果保留但不再作为后续重复模板，四个粒子流体 Demo 从本 change 后续常规验证中排除。Vulkan Preview 单批 3 次预热 + 5 次/180 帧测量完成；相对 P1 单批出现 CPU frame/loop 与 Game GPU 约 6% 的观察信号、Scene GPU 通过，第二批在用户要求停止过度验证后中止，不伪报正式双批通过。阶段源码恢复点 `20260830-p2-rendergraph-boundaries` 已封存 948 文件；后续不重复全源码哈希。RenderGraph 公共 API、HPWater/Fluid/Ocean/shader/Demo 算法未改，可以进入 P3。

## 4. P3 — 建立 Feature 生命周期与类型化图契约

- [x] 4.1 新增 `RenderFeature.h`、`RenderFeatureContext.h` 和 `RenderFeatureLifecycleTests.cpp`，扩展现有 Registry 的注册描述；验证：稳定 ID、shared/view scope、必要/可选操作与初始化前置条件测试通过，不引入强制大基类。
- [x] 4.2 实现依赖校验、稳定阶段排序、部分失败清理和幂等 Shutdown；验证：重复 ID、缺失依赖、依赖环、遗漏必要操作在 GPU 提交前失败，失败实例与已初始化依赖按正确顺序释放。
- [x] 4.3 在现有 Blackboard 上添加类型化发布约束及通用 `FeatureGraphTypes`，保留 RDG 显式读写；验证：缺输入、错误类型、冲突生产者、过期 generation/version 和跨视图句柄测试失败，可选输入的显式后备测试通过。
- [x] 4.4 新增 `BuiltInRenderFeatures` 组装层与阶段接线，先适配一个低耦合 Feature 并提供短期 legacy adapter；验证：该功能的启用/禁用、Pass 顺序和输出与 P0 一致，组装以外的调度器不依赖其具体实现。
- [x] 4.5 将 WaterOptics 输入输出移至 `Ocean/WaterOpticsGraph.h` 并适配现有生命周期；验证：启用时 opaque depth 与 composite depth 不混用，HDR/运动输出读写版本正确，未启用 HPWater 的海洋场景不新增专用光学 Pass，算法与 shader 不变。
- [x] 4.6 接入 WaterOptics 的每视图 resize/reset/scene/config 事件及资源退役；验证：单视图 camera cut 不清空另一视图历史或共享谱，在途 resize/质量切换的旧资源完成后回收，双后端无新增 descriptor/validation 错误。
- [x] 4.7 将 Fluid 图契约移至 `Fluid/FluidGraph.h` 并适配其已核实的原作用域；验证：使用无窗口 Fluid 图 fixture 固定 PBF/渲染 Pass、reset、尺寸变化和 shared/view scope，不运行四个粒子流体 Demo，也不擅自改成跨视图共享模拟。
- [x] 4.8 适配 ClusteredLighting 与 LocalLightShadows 的阶段、资源及生命周期；验证：开启/关闭、光源变化和尺寸变化的图计划与图像对照通过，原 PSO 与阴影算法不变。
- [x] 4.9 适配 VarianceShadowMaps 与 PlanarReflections；验证：资源输入输出、历史/尺寸通知和相关场景回归通过，各自 scratch 不进入通用 FeatureGraphTypes 全集。
- [x] 4.10 适配 ScreenSpaceEffects 与 TemporalAntiAliasing；验证：history、camera cut、当前/前一矩阵以及后处理顺序对照通过，Game/Editor 历史互不污染。
- [x] 4.11 适配 SkyAtmosphere；验证：天空启停、参数更新、资源重建与既有主干阶段顺序测试通过，低耦合试点已覆盖时补齐其全部事件路径而不重复实现。
- [x] 4.12 适配 FftOcean、SpectralOceanSimulation 和 LocalWaveGpuResources 的注册/图输出；验证：资源仍由原共享 owner 保活，FFT/foam/local-wave 参数、纹理与 Pass 链对照通过，不创建重复模拟实例。
- [x] 4.13 适配 InteractiveTerrain 与 VirtualTextureCache 的生命周期；验证：更新、reset、场景切换、驻留和退役测试通过，地形算法与虚拟纹理策略不变。
- [x] 4.14 适配 GpuDrivenVisibility；验证：GPU object records、实例偏移、readback、禁用后备及尺寸/场景切换对照通过，保留当前 GPU-driven 行为。
- [x] 4.15 由 RenderFrameCoordinator 用 LogicalFrameId 选择一次共享生产，统一 shared/view 事件路由；验证：双视图每逻辑帧一次模拟、帧槽回绕、默认生产视图不可用、无消费者、隐藏后恢复全部通过，GPU 消费依赖不能仅由 CPU 标志替代。
- [x] 4.16 新增仅供测试的独立阶段 Feature，验证扩展与错误隔离；验证：接入既有阶段无需新增 shared resources/callback 全集字段，错误输入可定位 Feature/资源，既有功能输出及禁用逻辑不受影响。
- [x] 4.17 清理全部已迁移的 legacy shared-struct 字段、adapter 和重复接线；验证：调用者检索为零，所有内置 Feature 接入点均登记完成，SceneRenderer 保留合理几何/主干职责而不隐藏新全集。
- [x] 4.18 执行 P3 生命周期集成验收；验证：Feature 规格由直接 fixture 覆盖，运行风险代表 Demo 与 HPWater 必要双视图/时域/队列样本；排除四个粒子流体 Demo，不重复全 Demo 或双批 V3。

4.1 已完成，当前 **49/128**：新增无继承要求的 `RenderFeatureRegistration`、操作专用 context、shared/view scope、稳定字符串 ID、阶段、依赖、必要操作和初始化前置条件描述；现有 Registry 增加生命周期注册重载及只读查询，legacy 注册/Apply/Blackboard 路径保持不变。新增 `PrismRenderFeatureLifecycleTests` 直接覆盖描述保存、两种 scope、必要回调存在、可选回调省略及普通非多态结构；仅构建并运行该目标，测试通过，未启动 Demo、GPU/性能矩阵或源码哈希。下一项是 4.2 注册有效性、依赖顺序与失败清理。

4.2 已完成，当前 **50/128**：Registry 在初始化前解析生命周期图，拒绝重复 ID、缺失必要依赖、依赖环和遗漏的必要回调；可选缺失依赖仍允许。可运行节点按最早阶段和注册顺序稳定选择，显式依赖优先于阶段。初始化失败先清理失败实例已取得的资源，再按解析顺序逆序释放已成功初始化的依赖，同时保留原始初始化异常；正常 Shutdown 即使回调异常也继续释放其余实例，并在首次调用前移出活动列表，保证重复 Shutdown 不重复销毁。扩展同一直接 fixture 覆盖上述错误、排序、失败回滚及正常逆序幂等释放；仅重建/运行 `PrismRenderFeatureLifecycleTests`，无警告且通过，未运行 Demo、GPU/性能/哈希矩阵。下一项是 4.3 类型化图发布约束。

4.3 已完成，当前 **51/128**：复用 `RenderGraphBlackboard` 增加由 slot 类型和 value 类型共同约束的 Feature 发布接口，记录 producer、shared/view scope、ViewId、graph generation 和显式 value version。首次 `Publish` 拒绝冲突生产者，`PublishNext` 只允许同 producer/scope/view/generation 且版本单调前进；`Require` 拒绝缺失、错误类型、过期 generation/version 与跨视图访问，`GetOptionalOr` 仅在真正缺失时返回调用方显式后备。新增 `FeatureGraphTypes.h` 只包含 Opaque/Lit/Composite 通用场景契约，不吸收 Feature 私有 scratch；legacy `Set/Get` 与 RDG Read/Write 语义保留。直接契约测试与一个代表性 RenderGraph 生产目标构建/测试通过；未运行 Demo、GPU、性能或哈希矩阵。下一项是 4.4 内置 Feature 组装层与低耦合试点。

4.4 已完成，当前 **52/128**：新增 `BuiltInRenderFeatures.{h,cpp}`，把具体内置 Feature 的组装知识从 `SceneRenderer` 注册序列移入单一组装层；首个试点为 view-local `SkyAtmosphere`，稳定 ID 与原诊断名称保持 `SkyAtmosphere`，显式参与 `FramePreparation`。Registry 新增按阶段执行已初始化 graph operation 的通用入口，SceneRenderer 只调用生命周期阶段，不依赖试点具体类型；Game/Scene 分别携带稳定临时 ViewId 1/2。短期 legacy adapter 只负责把该 operation 转接到既有 SharedRenderGraphResources/Callbacks，标注由 4.17 删除；Atmosphere 的启停仍由原 ScenePipelinePlan/Frontend 决定，原 `AtmosphereTransmittance`→`AtmosphereSkyView`→场景 Pass 顺序及输出 version 保持。生命周期 fixture 覆盖阶段隔离、资源发布、ID/scope/诊断；既有 RenderGraph fixture 覆盖关闭 17 Pass 与开启 19 Pass、前两 Pass 名称及 LUT version。仅构建/运行这两个目标，均通过；未运行 Demo、GPU、性能或哈希矩阵。下一项是 4.5 WaterOptics 自有图契约。

4.5 已完成，当前 **53/128**：`WaterOpticsGraphInputs`、`WaterOpticsGraphResult` 与回调类型移入 `Renderer/Features/Ocean/WaterOpticsGraph.h`，共享前端只包含这一 Feature-owned 契约；私有 scratch/history 仍由 `WaterOpticsFeature` 持有。内置组装层以稳定 ID `WaterOptics`、view-local scope 和 `WaterOptics` 阶段注册 graph operation，SceneRenderer 传入当前 view 的尺寸、帧索引和 scene，并移除原每帧手工注册块。原 ScenePipelinePlan 仍决定是否执行：关闭 HPWater 时不调用该回调且不产生 WaterOptics Pass；开启时既有图测试继续确认 opaque depth 保持独立、transparent 使用 composite depth、HDR/motion/composite 输出版本与 Pass 顺序不变。仅构建/运行生命周期和 RenderGraph 两个目标，均通过；未改算法/shader，未运行 Demo、GPU、性能或哈希矩阵。下一项是 4.6 WaterOptics 每视图事件与资源退役。

4.6 已完成，当前 **54/128**：Registry 增加统一的 PrepareFrame、Resize 与 SceneChanged 分发，WaterOptics 注册为 view-local 并声明 prepare/build/resize/scene/shutdown 操作。每帧配置分类直接比较结构字段而非哈希；启停、质量/分辨率变化通过已有 frame-ring retirement 保活旧资源，移除质量切换的全 GPU `WaitForGpu`，resize 只在调用方已完成 GPU-idle 的 swapchain 安全点立即释放。camera cut、surface/history reset 仍以当前 renderer 独有版本进入历史 key，scene 通知也按 Game/Scene 各自 Registry 分发，不触碰共享谱模拟。生命周期 fixture 新增双 Registry 隔离断言；生命周期、RenderGraph、WaterOptics CPU 测试和完整 `PrismRender` 构建通过。定向 GPU fixture 的 D3D12 validation 通过；Vulkan validation 因本机实例创建前缺失验证层返回 `VkResult=-6`，按用户要求不再修改注册表追查，关闭验证层的同一 Vulkan fixture 通过。未运行 Demo、粒子水场景、全场景或哈希矩阵。下一项是 4.7 Fluid 自有图契约。

4.7 已完成，当前 **55/128**：新增 `Renderer/Features/Fluid/FluidGraph.h`，以 `FluidGraphInputs/Result/Callback` 封装 scene color、实际透明深度和共享 environment；PBF buffers、重建 scratch、caustics 继续由 Fluid 私有持有。`BuiltInRenderFeatures` 以稳定 ID `Fluid`、view-local scope、PostProcess 阶段注册现有图入口，SceneRenderer 删除旧固定 Registry 回调并只调用通用阶段。Fluid 仍是每个 SceneRenderer 的值成员，Resize、scene reset、Update、EndFrame 以及 PBF/重建/caustics Pass 实现均未改，也未提升为跨视图共享模拟。无窗口 RenderGraph fixture 验证 Fluid 读取同一 environment、正确 scene color/depth 并发布新 color，生命周期 fixture 验证错误阶段不注册、PostProcess 注册以及 view-local scope；两个目标通过。按用户要求未运行四个粒子流体 Demo、场景矩阵、GPU 套件或哈希验证。下一项是 4.8 ClusteredLighting 与 LocalLightShadows 生命周期适配。

4.8 已完成，当前 **56/128**：`ClusteredLighting` 与 `LocalLightShadows` 已从 SceneRenderer 的旧固定 Registry 迁入 `BuiltInRenderFeatures`，分别声明 view-local `Lighting`/`Shadows` 阶段；Clustered resize 通过统一 GPU-idle 事件执行，并以 Registry 初始化状态保证初始化/运行时各只重建一次。原 Clustered→Local 每帧 Update、EndFrame、PSO、阴影缓存和算法保持不变。此前 `shadows` 的 TAA RTV 失败来自 `SceneRenderer.h` 更新后多个旧对象文件未重编，导致 `SceneRendererPasses.cpp.obj` 按过期成员布局访问对象；目标级 clean rebuild 后同一 2 帧探针退出 0。生命周期与 RenderGraph 直接测试通过；D3D12/native/strict 第 30 帧的 graph 含 `ClusteredLightBuild`、`SpotShadows`、`PointShadows`，相对 P0 同后端 shadows 基线逐像素一致（MAE/RMSE/change/max error 0，SSIM 1）。失败探针继续保留，不修改 TAA、PSO、阴影算法、shader 或 Demo 默认值；按用户要求未运行全 Demo、四个 Fluid Demo、性能或全仓哈希。下一项是 4.9 VarianceShadowMaps 与 PlanarReflections。

4.9 已完成，当前 **57/128**：`VarianceShadowMaps` 与 `PlanarReflections` 已迁入 `BuiltInRenderFeatures`，保持 view-local `Shadows` 阶段；固定 1024×1024×3 的 Variance 只声明 BuildGraph，随视图尺寸变化的 Planar 同时声明 GPU-idle Resize。初次资源创建仍走原路径，运行时 resize 只经生命周期执行一次，后续 ScreenSpaceEffects 继续取得同一新 Planar color；两项 Update/EndFrame、状态历史、PSO、scratch 和 Pass 算法未改，moments/scratch 及 reflection depth 均未进入 `FeatureGraphTypes`。生命周期与 RenderGraph 直接测试通过；D3D12 `shadows` 2 帧 graph 保留 `ShadowMoments`/双向 blur，`reflections` native/strict 第 30 帧保留 `PlanarReflection`，相对 P0 同后端基线逐像素一致（全部误差 0、SSIM 1）。未运行全 Demo、四个 Fluid Demo、性能或全仓哈希。下一项是 4.10 ScreenSpaceEffects 与 TemporalAntiAliasing。

4.10 已完成，当前 **58/128**：`ScreenSpaceEffects` 与 `TemporalAntiAliasing` 已迁入 `BuiltInRenderFeatures`，分别声明 view-local `PostProcess`/`Temporal` 阶段以及 Planar→ScreenSpace→TAA 的必要依赖。运行时尺寸事件移到新 GBuffer/HiZ/HDR 建立之后，再按依赖重建 Planar、ScreenSpace 和 TAA，避免绑定旧输入且各只执行一次；初始资源建立仍走原路径。TAA 的 scene-change history reset 改由生命周期事件分发，原 camera-cut 判定、当前/前一 world-view-projection 更新、禁用分支 reset、EndFrame ping-pong、shader 与 PSO 均未改。生命周期 fixture 固定 resize 顺序、scene reset 和两个独立 Registry 的 Game/Editor history 隔离；共享 RenderGraph fixture 显式固定 ScreenSpaceReflections→TemporalResolve→BloomExtract→Tonemap。两项直接测试通过；D3D12/native/strict 的单个 `post-process` 第 30 帧样本退出 0，图为 TemporalResolve→BloomExtract→BloomHorizontal→BloomVertical→Tonemap，相对 P0 同后端基线逐像素一致（全部误差 0、SSIM 1）。仅有既有 D3D12 warning 820；未运行全 Demo、四个 Fluid Demo、性能或全仓哈希。下一项是 4.11 SkyAtmosphere。

4.11 已完成，当前 **59/128**：4.4 的 `SkyAtmosphere` 低耦合 BuildGraph 试点补齐 `PrepareFrame`，SceneRenderer 中原每帧 LUT 参数更新改由统一生命周期分发，保持 view-local `FramePreparation` 阶段和原 Transmittance→SkyView 主干。固定尺寸 LUT、初始化、EndFrame 状态提交、关闭天空时黑色后备、shader/PSO 均未改；因 LUT 为固定 256×64/192×108 且每个启用帧重新生成，未伪造无意义的 view Resize 或 SceneChanged 操作。生命周期/RenderGraph 直接测试通过；D3D12 strict 的单个 Sky 专项 fixture 覆盖两个 View 独立启停、全部帧槽、参数谓词及两次窗口 Resize，退出 0；单个 `atmosphere` native/strict 第 30 帧图以 AtmosphereTransmittance→AtmosphereSkyView 开头，相对 P0 同后端基线逐像素一致（全部误差 0、SSIM 1）。仅有既有 warning 820；未运行全 Demo、四个 Fluid Demo、性能或全仓哈希。下一项是 4.12 Ocean 图输出生命周期接入。

4.12 已完成，当前 **60/128**：`FftOcean`、`SpectralOcean` 与 `LocalWave` 的旧固定 Registry 回调已迁入 `BuiltInRenderFeatures`，稳定 ID 和实现选择谓词不变，统一声明 `FramePreparation` 图阶段。Legacy FFT 保持每个 SceneRenderer 自有的 view-local 实例；Spectral/LocalWave 注册为 device-shared borrowed reference，唯一 owner 仍是 `SceneRendererSharedResources`，Registry 不负责初始化、关闭或复制实例，LocalWave 显式依赖 SpectralOcean。现有 `oceanSimulationEnabled` 生产门禁继续使 secondary view 只导入/采样共享图，不生成第二条谱/FFT/foam/local-wave Pass 链。生命周期与 RenderGraph 直接测试通过；D3D12 native/strict 的 `ocean` 第 30 帧 Legacy FFT 相对 P0 逐像素一致，`waveworks-ocean` 图只含 7 个 Spectral 与 2 个 LocalWave Pass、无 Legacy FFT Pass，图像以原 V2 门限通过（MAE 0.000003、RMSE 0.000283、changed 0.000021、SSIM 0.999996）。仅有既有 warning 820；未改 Ocean/HPWater 算法、shader、owner、Update/EndFrame 或 Demo 设置，未运行全 Demo、四个 Fluid Demo、性能或全仓哈希。下一项是 4.13 InteractiveTerrain 与 VirtualTextureCache。

4.13 已完成，当前 **61/128**：`InteractiveTerrain` 与 `VirtualTextureCache` 已由 `BuiltInRenderFeatures` 以稳定 ID 接入统一 `FramePreparation`。共享地形保持 `SceneRendererSharedResources` 唯一 owner 和 device-shared borrowed 生命周期，场景事件只清除命令 revision 水位并在下一次启用执行时原地初始化高度图；每视图 VT 保持原 CPU LRU/页表/atlas 策略，prepare 仍使用同一相机、world size 与启用谓词。VT atlas 替换现在按 frame slot 保留旧 texture/view，只有 BeginFrame 已等待并回收该槽时才释放；场景 reset 延迟到该安全槽执行，当前槽 descriptor 在所有 prepare 完成后统一改写。新增 `PrismVirtualTextureCacheTests` 以 mock RHI 固定 49 页请求半径、miss/residency、reset generation、两帧退役上界和旧 atlas 精确释放；生命周期与 RenderGraph 直接测试也通过。D3D12 native/strict 单个 `terrain-vt` 第 30 帧图只含一次 `InteractiveTerrain.BrushAndErosion`，相对 P0 同后端基线逐像素一致。测试中发现中文 MSVC `/showIncludes` 前缀尾随空格使 Ninja 记录 0 个头依赖并混入旧类布局；CMake 现固定实际 UTF-8 前缀且无尾随空格，重新构建后目标显示 84 个头依赖。只有既有 warning 820；未改地形/VT shader、算法、Demo 默认值，未运行全 Demo、四个 Fluid Demo、性能或全仓哈希。下一项是 4.14 GpuDrivenVisibility。

4.14 已完成，当前 **62/128**：`GpuDrivenVisibility` 的旧固定 Registry 回调已迁入 `BuiltInRenderFeatures`，保持 view-local `FramePreparation` 和稳定 ID。原对象记录构建、`firstInstance = object index`、batch argument offset、compute/indirect 状态和帧槽 readback 算法未改；PrepareFrame 仍在建图前更新同一 Scene/Camera/extent，Resize 在调用方 GPU-idle 后切换新 HiZ，SceneChanged 只丢弃旧对象索引/相机关联的迟到诊断而不提前释放 GPU buffer。新增 `PrismGpuDrivenVisibilityGpuTests`：Vulkan 实际执行 GpuVisibility 并覆盖多帧 object/readback、关闭 CPU 后备、640×360 resize 和 GpuDriven→Preview 场景切换；D3D12 保持当前 Shader Model 6 object-index 限制下的 CPU 后备，两项均通过。生命周期与 RenderGraph 直接测试通过。单个 Vulkan 启用样本图包含 `GpuVisibility`、`IndirectArguments` 和 `IndirectDrawCounts`；P0 固定图因 deterministic 模式关闭 GPU-driven，不能与显式启用样本混比，失败诊断保留。相同 deterministic/fallback 协议的第 30 帧相对 P0 逐像素一致（全部误差 0、SSIM 1）。未运行全 Demo、四个 Fluid Demo、性能或全仓哈希；下一项是 4.15 LogicalFrameId 共享生产选择与事件路由。

4.15 已完成，当前 **63/128**：`RenderFrameCoordinator::BeginLogicalFrame` 以单调 `LogicalFrameId` 为身份、以 frame slot 仅作循环资源索引，每逻辑帧只选择一次 device-shared producer；Game 为默认生产者，不可用时回退 Scene，无消费者时不选择，隐藏/恢复和槽位回绕不会重复决策。SceneRenderer 每次显式 Render 必须先取得协调器分配，Feature Prepare/Resize/SceneChanged 已按 device-shared/view-local scope 分发；共享 Spectral/LocalWave/InteractiveTerrain 只有选中视图记录 mutation，secondary view 仍导入相同共享纹理并在 GBuffer/Shadow 中声明 ShaderResource 读取，GPU 顺序不依赖 CPU 标志猜测。定向 RenderFrameCoordinator、FeatureLifecycle、RenderGraph 三项测试通过；D3D12 `waveworks-ocean` 双视图连续 30 帧中 Game/Scene LogicalFrameId 均匹配 1..30，槽位 0/1 回绕，Game 每帧 8 类共享模拟 Pass、Scene 为 0。1280×800 第 30 帧相对 P0 同后端基线 MAE 0.000001、RMSE 0.000123、changed 0.000004、SSIM 0.999999。未运行全 Demo、四个 Fluid Demo、性能或全仓哈希；下一项是 4.16 独立测试 Feature。

4.16 已完成，当前 **64/128**：新增仅位于 `tests/IndependentRenderFeatureFixture.h` 的独立 PostProcess Feature，它通过 test-local slot/value 类型从 Blackboard 读取输入、发布独立输出并登记自己的 Pass，整个生产代码没有新增 `SharedRenderGraphResources` 或 `SharedRenderGraphCallbacks` 字段。fixture 同时保留一个既有 Temporal typed output，证明扩展前后 token 不变；关闭新 Feature 时不请求输入、不发布输出且不产生 Pass。`RenderGraphBlackboardRequest` 新增可选的人类可读资源名，缺失、类型、代次、版本和跨视图错误现在同时报告 consumer Feature ID 与资源名，slot/value RTTI 仍是权威类型身份。`PrismFeatureGraphContractTests` 与 `PrismRenderFeatureLifecycleTests` 直接通过，完整 `PrismRender` 重新编译链接通过；未运行 Demo、GPU、性能或哈希矩阵。下一项是 4.17 清理 legacy adapter 与重复接线。

4.17 已完成，当前 **65/128**：ClusteredLighting、LocalLightShadows、VarianceShadowMaps、PlanarReflections、ScreenSpaceEffects、TemporalAntiAliasing、FftOcean、SpectralOceanSimulation 与 LocalWave 的剩余资源和回调均迁移到类型化 contribution；15 个内置 Feature 接入点全部由 `BuiltInRenderFeatures` 登记。删除 Registry legacy adapter、旧 shared-struct Feature 字段、重复 Blackboard 发布及 SceneRenderer 接线后，`legacyResources`、`legacyCallbacks`、`RegisterLegacyAdapter`、`RenderFeatureRegistrationContext`、`RegisteredRenderFeatures` 与 `PublishToBlackboard` 的生产调用者检索为零。`SharedRenderGraphResources/Callbacks` 只保留场景几何、主阴影、GBuffer、主照明、HiZ、OceanQuery、Bloom/Tonemap/Output 等渲染主干契约，SceneRenderer 未形成替代全集。定向构建 `PrismRender`、`PrismRenderFeatureLifecycleTests`、`PrismRenderGraphTests` 通过，两个直接测试均退出 0；未运行 Demo、GPU、性能或哈希矩阵。下一项是 4.18 P3 生命周期集成验收。

4.18 已完成，当前 **66/128**，P3 收口：Feature 生命周期与图契约由 `PrismFeatureGraphContractTests`、`PrismRenderFeatureLifecycleTests`、`PrismRenderFrameCoordinatorTests` 和 `PrismRenderGraphTests` 的既有定向 fixture 覆盖，4.17 后重新通过其中直接受影响的生命周期与 RenderGraph 两项。当前 D3D12 `waveworks-ocean` native/strict 连续 30 帧样本记录 Game 1..30、按需 Scene 同一 LogicalFrameId、Game 单 producer/Scene 零 producer，请求 native 且实际为 `dag_multi_queue`；第 30 帧相对 P0 为 MAE 0.000001、RMSE 0.000123、changed 0.000004、SSIM 0.999999。HPWater 与冻结 P2 完全同配置的 underwater/native/strict 330 帧 lifecycle 完成，frame 320 恢复断言与 coverage 0.7886064648628235 均成立，图像为 MAE 0.000070、RMSE 0.003398、changed 0.000403、SSIM 0.999793；无 warning 820 之外的 validation 信息。首次 default-camera 样本仅用于发现与基线配置不一致，明确不参与回归结论。未运行全 Demo、四个 Fluid Demo、Vulkan、性能双批或全仓哈希；下一项是 5.1 RenderView/ViewFamily 数据边界盘点。

5.1 已完成，当前 **67/128**：新增默认关闭的 `RenderSceneExtractionStatistics` 与一次性报告写出器，显式把 scene-data build/reuse、mailbox envelope、未来 FramePacket 小对象、publication/Game/Scene 完整复制次数、估算对象/字节数及三段 CPU 时间分开；GPU 资产只按 shared binding 值计入 `RenderObject`，不把共享 GPU allocation 误报为深复制。`ApplicationHost` 仅在 `PRISM_RENDER_SCENE_PUBLICATION_STATS_PATH` 启用时扫描对象/字符串，普通路径不增加 O(N) 统计开销；现有 mailbox、CreateGameView/CreateEditorView 和渲染输入未改。新增 `Benchmark-ScenePublication.ps1` 以确定性有限帧输入生成 plan/report，并校验 legacy full-copy 基线。D3D12 `preview` 两次 8 帧结果结构一致：每帧 6 对象，8 publication + 8 Game + 16 Scene 完整复制，合计估算 192 对象/250272 字节，build 8、reuse 0、FramePacket 小对象 0；最终二进制 2 帧样本为 2/2/4 次复制和 62568 字节。`PrismRender` 与 `PrismWorldRenderSceneBridgeTests` 构建通过，场景桥测试退出 0；未运行全 Demo、Fluid、GPU validation、性能分布或哈希。下一项是 5.2 定义版本化 RenderSceneData/RenderView/RenderFramePacket 类型。

## 5. P4 — 版本化场景数据与独立 RenderView

- [x] 5.1 新增 `RenderSceneExtractionStatistics` 和 `Benchmark-ScenePublication.ps1`，先度量现有发布/双视图复制；验证：对象/字节复制估算、构建/复用计数、CPU 时长和 frame-packet 小对象计数分开，固定输入能复现基线而不改渲染结果。
- [x] 5.2 定义 `RenderSceneData`、`RenderView`、`RenderFramePacket` 及 scene/data/view/logical-frame 标识；验证：类型与生命周期测试通过，WorldRenderSnapshot/文件格式不变，静态对象、动态帧值和每视图值归属明确。
- [x] 5.3 为程序化/World 对象建立稳定 ID、父子和实例索引映射；验证：对象新增/删除、拓扑重建和 Game/Editor 选择测试通过，过滤不改变共享数组中的 parent index 或 firstInstance。
- [x] 5.4 实现快照 CPU 参数值化与 AssetRegistry 运行时绑定版本；验证：材质/mesh/texture 替换后旧快照参数与绑定不被原地改变，既有序列化 handle 格式不变，底层动态 GPU 内容仍受原同步规则约束。
- [x] 5.5 新增 `SceneChangeTracker` 与提交范围，接入 World 命令和 bridge；验证：实体/层级/变换/可见性/材质变更及 transaction/rollback/undo/redo/replay 测试通过，一批提交不发布中间混合状态。
- [x] 5.6 接入资产上传完成、reimport/eviction、streaming scene activation 和 Demo/glTF/程序化工厂变更；验证：每类变更触发正确 scene/data/binding revision，旧帧安全使用旧绑定，未知变更保守重建并记录原因。
- [x] 5.7 将 Editor、无 Editor 导航及 WaterValidationSequence 的相机/灯光/设置更新接入小型动态输入；验证：仅相机更新不改对象版本，两个视图相机隔离，影响历史的设置触发对应事件而不冻结模拟或水下判定。
- [x] 5.8 实现 `RenderSceneExtractor` 的 full-rebuild 与 versioned 两策略以及诊断配置，暂以 full-rebuild 为默认；验证：相同 mutation 序列得到等价对象/绑定结果，未知写入出现可见 fallback，模式配置有默认值及无效值测试。
- [x] 5.9 将 mailbox 改为发布自洽 FramePacket 并复用未变数据，保留小互斥锁和当前线程模型；验证：旧消费者保活、连续发布、切场景及无人持有后的回收测试通过，不保存无限历史版本。
- [x] 5.10 迁移 SceneRenderer、draw 构建及 FeatureContext 消费新 packet/view，移除 Game/Editor 全量 RenderScene view 拷贝；验证：几何、灯光、透明、运动矩阵和光学各消费正确视图，强引用覆盖 graph lambda/并行录制引用周期。
- [x] 5.11 将 GPU 可见性反馈与 Editor 辅助显示改为版本化每视图映射；验证：旧场景/旧 data revision/错误 ViewId 的迟到反馈被拒绝，地形双视图选择正确，不再修改已发布 RenderObject。
- [x] 5.12 补齐 `RenderScenePublicationTests.cpp` 的完整 mutation 矩阵和旧帧资源测试，审计全部 raw mutable 入口；验证：design D5 的每个来源都有测试或显式保守后备，事务失败不泄漏状态，静态路径不依赖每帧 O(N) 哈希。
- [x] 5.13 运行 full-rebuild/versioned 等输入 GPU 对照及保活压力测试；验证：HPWater 时间推进、双视图历史、terrain、反复切场景/resize 和并行录制通过，Fluid 仅用模块 fixture，旧 CPU/GPU 资源最终回收且无无界内存增长。
- [x] 5.14 运行静态、仅相机和对象持续编辑的复制/性能验收；验证：前两者预热后至少 120 帧完整对象复制与 build 增量为零，真实变更批次最多重建一次，动态场景不伪报复用，V3 分布与门限通过。
- [x] 5.15 在 5.12–5.14 通过后切换默认 versioned，删除旧帧输入适配但保留显式 full-rebuild 诊断策略；验证：无配置时使用复用，显式后备仍可复现等价输出，旧接口调用者为零且已有 CLI/文件/报告兼容。
- [x] 5.16 完成 P4 验收记录和可恢复检查点；验证：快照规格所有场景关联到测试/报告，P0 与新默认路径图像、生命周期、性能对照通过，回退到 full-rebuild 的操作已验证后进入 P5。

## 6. P5 — 用 CMake target 与公开头文件规则落实分层

- [x] 6.1 编制 `cmake/PrismModuleBoundaries.json`，明确生产源 owner、公开/内部头文件、direct edge 和测试 fixture 例外；验证：与 P1–P4 后源码逐项核对，包含 Core/Application、FileDialog、Diagnostics 和 UI backend 的实际归属，不按目录粗判依赖。
- [x] 6.2 新增 PrismCore/PrismPlatform 源清单与 targets，将基础工具/Window 从 RHI 中剥离；验证：RHI、Engine 及应用构建通过，Platform 不引入 Editor/FileDialog，通用 Core 不依赖 Renderer/应用策略。
- [x] 6.3 新增 PrismAsset target 并迁移源归属与第三方依赖；验证：Asset/ShaderCompiler/导入工具相关测试通过，Slang/JSON/tinygltf 的 include/link 可见性正确，生产源不再同时编入 Renderer。
- [x] 6.4 新增 PrismScene target，接入 SceneSession/Extractor/bridge 并保留必要 Engine/Asset/RHI/Platform 依赖；验证：场景/World bridge/发布测试通过，未为消除真实 GPU 工厂依赖引入额外架构改写或 target 环。
- [x] 6.5 建立 PrismApplication target，重接 ApplicationHost/Launcher/services、PrismRender 和 Diagnostics；验证：应用与命令行测试通过，Windows/POSIX 诊断实现及 Editor 条件依赖正确，可执行文件名与 main 入口不变。
- [x] 6.6 将 D3D12/Vulkan ImGuiRenderer 文件迁至 `UI/Backends` 并更新工厂/include/源清单；验证：Editor 两后端 UI 冒烟通过，无 Editor 构建无需 ImGui，RHI 不再依赖 UI，文件迁移不改变绘制实现。
- [x] 6.7 清理 Renderer/Engine/Editor/测试/Tools 的直接链接和源清单，移除以重复收集再 REMOVE_ITEM 维持生产归属的做法；验证：所有目标能独立按所声明依赖构建，没有重复生产源、无必要的 PUBLIC 传递依赖或 runtime DLL 遗漏。
- [x] 6.8 实现 `CheckModuleBoundaries.cmake` 和边界正反例测试；验证：RHI→UI、Renderer→Application、内部头越界、target 环、缺失直接依赖、重复/漏归属均使 fixture 失败，合法 Editor backend 与显式测试重编译例外通过。
- [x] 6.9 接入公开头 self-containment 编译、现有 Windows presets 和 Windows CI；验证：独立包含公开头的测试与依赖检查运行，原 Editor/D3D12/Vulkan/harness 开关及 Slang/DXC 部署规则保持有效。Linux preset/workflow 可保留为非阻塞兼容性入口。
- [x] 6.10 执行 P5 两个 Windows 构建组合验收；验证：windows-ci 与 windows-vulkan-only-ci 的 configure/build/CPU-safe test、公开头/模块边界检查及所需 D3D12/Vulkan 运行冒烟通过，不要求 Linux 环境。

6.10 已按用户明确的平台范围修订并完成：`windows-ci` 全目标构建完成，CPU-safe 30 项均有本轮通过结果；`windows-vulkan-only-ci` 全目标构建完成且 28/28 通过；两个配置的模块边界/公开头检查通过，D3D12 与 Vulkan preview 各 headless 运行 3 帧并正常退出。Linux 不再是本 change 的验收或阶段门禁。P5 完成，当前 **92/128**；可以进入 P6。

## 7. P6 — 常驻有界工作线程池

- [x] 7.1 新增 `ITaskExecutor`、`TaskScheduler`、`TaskGroup` 和 CPU 测试，定义 worker/队列上限、启动失败及关闭协议；验证：零/单/多任务、空组、重复关闭和部分 worker 启动失败可安全收拢。
- [x] 7.2 实现 inline executor 与同路径任务组接口；验证：异常、结果槽和完成条件与 pool 一致，可注入 fake executor 检查顺序，不产生第二套渲染实现。
- [x] 7.3 实现常驻 worker、有界队列、背压与小任务直执行；验证：峰值线程/队列有界，持续投递无每 Pass 新线程，关闭唤醒全部等待者。
- [x] 7.4 实现组级失败传播、取消与嵌套等待保护；验证：任务抛异常/并发关闭/worker 请求等待同池工作不死锁，组引用保活至全部任务退出，不静默吞异常。
- [x] 7.5 审计并隔离录制上下文、descriptor、PSO/cache、动态上传和 Feature 写入口；验证：允许并行清单有实际依据，不安全操作保留执行 lane，线程亲和断言与负例通过。
- [x] 7.6 将 GraphExecutor 逐 Pass `std::async` 接入 TaskGroup，保持单一结果槽和稳定收集；验证：乱序完成 fixture 的 Pass/batch/fence/回放顺序与原计划一致，失败帧不提交 GPU。
- [x] 7.7 增加 `PRISM_RENDER_TASK_EXECUTOR=inline|pool` 的启动解析及诊断；验证：默认暂 inline、非法值失败，既有 GPU queue mode 含义和行为不变。
- [x] 7.8 记录任务排队/录制/join 时间、worker 与队列峰值；验证：统计准确且不混入 GPU 时长，不增加每任务全局争用或无界 trace 存储。
- [x] 7.9 执行长期任务压力、录制失败、退出与 CPU 数据竞争检查；验证：无悬空 packet/context，支持环境运行竞争检测，不可用项如实登记。
- [x] 7.10 执行双后端 inline/池风险代表集对照；验证：代表 Demo 图像、必要连续帧、HPWater 队列/双视图/共享模拟计数通过，排除四个粒子流体 Demo且不重复双批 V3，GPU 同步算法不变。
- [x] 7.11 完成 P6 检查点与后备验证；验证：诊断开关可退回 inline，原逐 Pass async 路径已无调用者，全部池验收通过才进入 P7。

7.1 已完成，当前 **93/128**：新增 Core 公共 `ITaskExecutor`、`TaskScheduler` 与 `TaskGroup`。Scheduler 构造时固定并校验 worker/队列上限，使用常驻 worker 和有界 FIFO；关闭停止接收但排空已接受任务，重复关闭幂等。可注入 worker factory 在部分线程创建后失败时会唤醒并 join 已启动线程。TaskGroup 统一包装任务、保活共享完成状态并等待空/单/多任务。新增 `PrismTaskSchedulerTests` 在 windows-ci 与 windows-vulkan-only-ci 两配置直接通过，完整配置的新增公开头自包含与模块边界 CTest 通过。Renderer/RHI/GraphExecutor、HPWater、Ocean/Fluid、shader、Demo 和默认线程模式均未修改；下一项是 7.2 inline executor 与同路径任务组契约。

7.2 已完成，当前 **94/128**：`InlineTaskExecutor` 实现同一 `ITaskExecutor` 契约，TaskGroup 对 inline/pool 使用同一提交、结果槽、完成与异常上送路径。定向测试以四个固定结果槽和“首任务失败、后续任务仍收拢”同时验证两种 executor；注入的 manual fake 固定提交顺序并反序完成，TaskGroup 只跟踪完成条件而不改变 executor 顺序。两个 Windows 配置的 `PrismTaskSchedulerTests` 均通过；未新增第二套 GraphExecutor 或渲染实现，下一项是 7.3 常驻有界池的背压与小任务直执行。

7.3 已完成，当前 **95/128**：`TaskScheduler` 保持构造期固定 worker 数，队列容量达到上限时对生产者施加可唤醒背压；`RunInline` 通过同一 executor/TaskGroup 路径执行且关闭会等待已接受的 inline 工作。新增只读峰值诊断验证 256 个持续任务未产生逐任务线程，worker/队列峰值不超过配置；单 worker/单槽 fixture 证明满队列阻塞并在空间释放后恢复。RelWithDebInfo 与 Vulkan-only 的定向测试通过；测试同步使用已有 `condition_variable`，避免引入额外 atomic-wait 运行库。下一项是 7.4 组级失败、取消、并发关闭与嵌套等待保护。

7.4 已完成，当前 **96/128**：TaskGroup 保留并上送首个异常，失败或显式 `Cancel` 后跳过尚未开始的同组任务，并向已运行任务提供协作取消 token；共享组状态与任务捕获保活至全部 wrapper 退出。worker 对同一 executor 的嵌套阻塞等待会立即失败而非饥饿；TaskScheduler 并发关闭由单一调用者 join，其余调用者等待同一完成状态，满队列提交者会被唤醒并收到拒绝。直接绕过 TaskGroup 的异常也保存为可查询/重抛诊断，不再静默丢弃。两个 Windows 配置的线程定向测试以及完整配置的公开头/模块边界检查通过；下一项是 7.5 并行录制亲和与共享写审计。

7.5 已完成，当前 **97/128**：新增 `GraphParallelRecordingContract`，仅 `parallelRecordable` 不再足以进入 worker；调用点必须同时确认独立 command context、frame-stable binding、无 descriptor/PSO/cache/动态上传写、无 Feature 状态写。RenderGraph 对 barrier/alias 规划、native recording 创建、batch/replay/提交执行 lane 加入线程亲和检查，并在诊断中公开审计状态。非 Fluid 的既有命令只读 Pass 已显式列入审计清单；四个粒子 Fluid Demo 对应 callback 保守留在 execution lane。负例证明未审计 Pass 即使请求 parallel 仍在主执行 context/thread，RenderGraph 与 Compiler CPU fixture 通过；详细依据见 `docs/PARALLEL_RECORDING_AUDIT.md`。下一项是 7.6 用 TaskGroup 替换逐 Pass `std::async`。

7.6 已完成，当前 **98/128**：GraphExecutor 新增可注入 `ITaskExecutor` 的同实现入口，旧入口使用 inline executor 保持单线程后备；所有审计通过的独立录制进入一个 TaskGroup，统一保活、等待与首异常传播，逐 Pass `std::async/std::future` 已无调用。两 worker fixture 强制第二 Pass 先完成，最终 marker、Pass、Graphics→Compute batch/fence 顺序仍按编译计划；池内录制异常在任何 `BeginQueueBatch` 前中止，未发布部分 passInfos。RenderGraph/Compiler CPU fixture 通过；下一项是 7.7 启动配置与默认 inline 组合接入。

7.7 已完成，当前 **99/128**：新增 `TaskExecutorKind` 解析/工厂，`PRISM_RENDER_TASK_EXECUTOR` 空值和 `inline` 选择同路径 inline executor，`pool` 创建固定 worker/256 槽有界池，其他值在 ApplicationHost 初始化时失败。RenderFrameCoordinator 作为双视图组合 owner 只持有一个 executor，Game/Scene renderer 共用且先释放 renderer 再关闭 worker；SceneRenderer 将其注入同一 RenderGraph Execute 实现。启动诊断日志 `renderer.task_executor` 记录实际模式；`PRISM_RENDER_RDG_QUEUE_MODE` 仍独立决定 GPU queue 调度。主程序编译以及 TaskScheduler、RenderGraph、RenderFrameCoordinator 三个 CPU fixture 通过；下一项是 7.8 worker 统计。

7.8 已完成，当前 **100/128**：`ITaskExecutor` 提供固定大小累计统计快照；pool 在已有队列锁/完成记账区间累计提交、完成、排队和录制纳秒，记录 worker/队列当前值与生命周期峰值，不保存逐任务事件。GraphExecutor 单独累计 TaskGroup join 墙钟，ApplicationHost 每帧用前后快照差分填充既有 `FrameWorkerProfile` 与 Worker CPU lane，GPU timing 字段未参与计算；JSONL profiler 报告新增 workers.queue/executing/join/峰值字段。executor 完成回调用于先完成调度记账再唤醒 TaskGroup，消除了最后任务统计跨帧的竞态。主程序及 TaskScheduler、RenderGraph、FrameProfiler 定向测试通过；下一项是 7.9 压力与退出检查。

7.9 已完成，当前 **101/128**：单个 CPU 压力 fixture 通过 4 个常驻 worker/32 槽队列收拢 4096 个任务，提交/完成计数一致、结束时 active/queued 均为 0 且峰值未越界。它与同套件既有的长任务门闩、满队列关闭、并发 Shutdown、部分 worker 启动失败、异常取消、嵌套等待拒绝和 payload 弱引用回收共同覆盖退出与保活；RenderGraph 的 pooled recording failure 继续证明失败帧未开始 GPU batch。当前 Windows MSVC 环境没有 ThreadSanitizer/等价 CPU race detector，因此该项如实登记为工具不可用，未用普通测试冒充数据竞争检测通过。下一项是 7.10 双后端代表集。

7.10 已完成，当前 **102/128**：`Capture-HpWater.ps1` 增加只影响自动化环境的 `-TaskExecutor inline|pool`，以 30 个连续确定性帧对 D3D12/Vulkan 各执行一组 inline/pool HPWater 代表样本。D3D12 成对图像逐字节一致；Vulkan 同后端比较 mean error 0.000002、RMSE 0.000456、changed ratio 0.000011、SSIM 0.999996，通过原同后端门限。另以 pool/native 在两后端各执行 12 帧 live 双视图诊断：最终帧均有 `game+scene`，Game 是唯一 simulation producer，Scene 为 consumer，8 个共享 Ocean simulation Pass 均恰好记录一次，两个视图均实际应用并行录制。过程中发现并修复 7.8 worker profiler 的可用性语义：inline 不再向 unavailable worker 字段写非零时长，pool 的聚合 worker lane 不伪造单个线程 ID；新增定向测试。未运行四个粒子 Fluid Demo、未执行 V3 双批或全场景矩阵，GPU queue/synchronization 算法未改。

7.11 已完成，P6 完成，当前 **103/128**：真实捕获日志分别确认 `renderer.task_executor` 可选择 inline 与 pool，默认/显式 inline 后备保持同一 GraphExecutor 实现；`src/Core`、`src/Renderer` 已无 `std::async/std::future` 调用。TaskScheduler、RenderGraph、RenderGraphCompiler、RenderFrameCoordinator、FrameProfiler、公开头自包含与模块边界 **7/7** 定向通过，主程序 clean build 与双后端 pool 捕获通过，OpenSpec strict 通过。P6 满足进入 P7 的门禁。

8.1 已完成，当前 **104/128**：Application 模块新增 `RenderExecutionService`、`RenderFrameQueue` 与带 command/scene/view/frame 身份的 `RenderControlCommand`。Service 组合并独占 `IRenderExecutionTarget`，inline 和 threaded 都将同一 `RenderFrameQueueEntry` 交给唯一 `ExecuteEntry` 实现；Scene 仅提供不可变 `RenderFramePacket`，未增加对 UI/Application 的依赖。新增 `PrismRenderFrameQueueTests` 以 fake target 验证两模式的 `frame:1 → control:1 → frame:2` 顺序、execution lane、异常上送和 target 恰好一次关闭；主程序与新目标编译通过。真实 backend/RenderFrameCoordinator owner 按顺序留给 8.3 迁移。

8.2 已完成，当前 **105/128**：`RenderFrameQueue` 默认仅允许一项等待，与 consumer 当前执行项构成“一帧等待加一帧执行”；帧逻辑 ID、控制 ID 和统一 acceptance ID 单调，接受后严格 FIFO。阻塞 Push 使用条件变量，`TryPush` 为主线程泵窗口事件提供无阻塞背压入口；测试以门闩式 fake target 固定第一帧执行、第二帧等待、第三帧生产者阻塞，释放后 1/2/3 恰好消费一次。正常 Close drain 已接受项并唤醒空队列 consumer/满队列 producer；Fail 唤醒双方、保留首异常并计数取消项，队列和峰值均有固定上限，不使用 sleep 或 latest-wins 覆盖。

## 8. P7 — 独立渲染线程与可靠帧交接

- [x] 8.1 新增 RenderExecutionService、RenderFrameQueue、控制命令类型与测试；验证：inline/threaded 共用执行实现，Application 组合 owner，Scene 不依赖 UI/Application。
- [x] 8.2 实现一帧等待加一帧执行的有界 FIFO、接受编号、关闭/失败唤醒；验证：快生产慢消费不丢已接受帧、不重复消费、不无限增长，生产者背压可处理必要窗口事件。
- [x] 8.3 将 backend/frame context、RenderFrameCoordinator 与 GPU 资源创建/释放迁至执行 lane；验证：GLFW/窗口亲和仍主线程，各后端初始化失败、BeginFrame/EndFrame/呈现和退出轨迹正确。
- [x] 8.4 实现 scene/view epoch 和可靠 resize/reset/quality/scene switch 命令与 ack；验证：顺序、目标帧边界、乱序/迟到反馈、失败等待者均正确，不用 latest-only 统计通道丢命令。
- [x] 8.5 冻结帧时间、步长、有效设置和相机值，按实际渲染帧维护历史；验证：主线程 N+1 编辑不污染 N，海洋每逻辑帧一次，Fluid 保持原作用域，隐藏/恢复不跳模拟或错配运动矩阵。
- [x] 8.6 新增 UiDrawPacket 与纹理保活，拆开 ImGui 构建和后端绘制；验证：顶点/索引/命令副本跨帧不变，render lane 不访问可变 ImGui context，两后端 UI、菜单/面板/双视图通过，无 Editor 不引入 UI。
- [x] 8.7 迁移资产 IO 完成、GPU 上传、绑定 revision 反馈及场景工厂安全派发；验证：主线程不越权触碰 render owner，旧绑定保活，流送/eviction/reimport/切场景无失序或死锁。
- [x] 8.8 将 capture/报告/max frames/退出绑定已接受及实际完成帧；验证：队列积压、resize/reset 后截图和旧 epoch 完成准确，取消帧不报告成功，旧 CLI 和机器字段语义兼容。
- [x] 8.9 接入版本化可见性/统计反馈和主线程 UI 消费；验证：scene/data/view/frame 不匹配时拒绝，反馈不回写已发布对象，统计滞后不影响控制命令可靠性。
- [x] 8.10 实现 drain、取消、任务收拢、GPU 等待与亲和销毁；验证：满队列退出、最小化、设备错误、部分初始化失败、反复关闭和 resize 期间退出无死锁/资源提前释放，错误唤醒所有等待者。
- [x] 8.11 增加 `PRISM_RENDER_EXECUTION_MODE=inline|threaded`，默认暂 inline；验证：非法值失败，与 task executor/GPU queue 配置正交，同一代码路径可退回单线程执行。
- [x] 8.12 增加 main/render/worker trace、排队深度、输入至呈现延迟和帧身份诊断；验证：用 latch/barrier 证明 main N+1 与 render N 重叠，不能用积压或丢帧冒充提速。
- [x] 8.13 运行线程/命令/资产/UI 生命周期压力和 CPU 竞争检查；验证：保活集合最终回收，无无界内存，自动化有超时失败报告，不通过 sleep 掩盖竞争。
- [x] 8.14 对原始 P0、新 inline、inline+pool、threaded+pool 运行风险代表 Demo/必要连续帧/双后端/HPWater 矩阵；四个粒子流体 Demo 排除。正式线程默认切换仍执行多进程 V3，原画质和模拟步数回归阻止切换。
- [x] 8.15 仅在 8.13–8.14 完成后切默认 threaded+pool，保留显式 inline 后备；验证：无配置及显式各模式均通过，后备操作可重放，默认改变不改变 Demo 效果。
- [x] 8.16 保存 P7 检查点和完整跨线程所有权/控制协议记录；验证：新增规格场景均有测试证据，不宣称独立 RHI 线程或未经测量的 FPS 收益。
- [x] 8.17 使用同一 Profiler 对 inline、inline+pool、threaded+pool 的 Main/Render/Worker/GPU/Submit/Present/等待和输入至呈现延迟作 matched 对照，形成独立 RHI Thread 的实施/不实施决策记录；验证：只有 API 翻译/提交在 Render lane 上形成可分离主瓶颈时才建议新 change，本 change 不直接实现 RHI Thread，也不以线程数量宣称收益。

8.3 已完成，当前 **106/128**：新增生产 `RenderRuntimeExecutionTarget`，由 `RenderExecutionService` 在 execution lane 初始化并独占 backend 与 `RenderFrameCoordinator`；ApplicationHost 不再持有两者的 `unique_ptr`，仅保留默认 inline 迁移期的非拥有别名，并由线程亲和检查阻止跨 lane 访问。backend 初始化、renderer GPU 资源初始化、BeginFrame、EndFrame/Present、Resize+swapchain 资源重建和 WaitForGpu 已收敛到 target，销毁顺序固定为 GPU drain→coordinator→backend；窗口/GLFW 仍由 Host 主线程创建、轮询和最终销毁。`PrismRenderFrameQueueTests` 固定 inline/threaded 的 initialize→FIFO work→shutdown 同 lane 轨迹、初始化失败清理一次；D3D12/Vulkan Preview 各 3 帧正常退出，两后端 `backend` 故障注入均非零退出且各记录一次 wait/shutdown/completed。默认仍为 inline，生产 Frame/Control 派发尚未开放，避免在 8.4–8.9 值化协议完成前误启 threaded；未运行 Demo 矩阵、四个粒子 Fluid Demo、图像或源码哈希。下一项是 8.4 scene/view epoch 与可靠控制 ack。

8.4 已完成，当前 **107/128**：新增 `RenderControlTicket` 与每条命令独立的有界 completion state；Service 不保存未领取 ack 的全局 map。BeforeFrame 必须位于对应下一帧之前且不能被其他帧越过，AfterFrame 必须紧随已接受/执行的目标帧；command ID、scene epoch、view epoch 单调推进，scene switch 显式宣布下一 epoch，resize 才能推进 view epoch，过期/跳跃输入在接受前失败。target ack 必须逐字段匹配 command/frame/scene/view 身份；显式业务拒绝返回 `succeeded=false`，异常和队列 Fail 会唤醒 ticket 并上送原异常。生产 target 在 execution lane 实际执行 resize，并可靠记录 reset、quality revision、scene epoch、capture request 与 stop 状态，供 8.5/8.8 在目标帧消费。定向测试覆盖 inline/threaded 顺序、边界、epoch、错误 ack、拒绝 ack、排队控制在前帧失败后的取消唤醒；主程序构建通过。未运行 Demo、GPU/图像矩阵或哈希；下一项是 8.5 冻结帧值并消费待应用控制。

8.5 已完成，当前 **108/128**：`RenderFramePacket` 现在显式值化 simulation time 与 step，动态输入每帧清除一次性 history invalidation，避免旧失效标志跨帧重复消费。`FrameEnvelope` 冻结 scene/view/settings identity、逐视图 active 状态及完整 `RenderSettings` 值；队列要求它们与可靠控制宣布的 epoch/revision 精确匹配。新增 `RenderFrameExecutionState` 只复制小型 view 数据，并以各视图上一实际完成渲染的 camera 覆盖 producer previous-camera；隐藏视图不推进历史，reset/resize/quality/scene-switch 的失效保留到该视图实际恢复渲染，取消的 prepared frame 不提交历史。生产 target 已实现同一 execution-lane 帧入口：应用冻结设置、每逻辑帧调用一次 Coordinator 生产选择、按 active plan 渲染 Game/Scene、处理 frame-bound swapchain 恢复并在执行完成后提交历史；Fluid 仍是每个 SceneRenderer 的原 view-local 实例。定向测试覆盖 N+1 设置隔离、伪 previous-camera 覆盖、隐藏/恢复、可靠 reset、step 错配拒绝和 epoch/settings 消费；FrameQueue/SceneSession/RenderScenePublication/公开头/模块边界 5/5 通过，D3D12 headless preview 仅 3 帧正常退出。未运行全 Demo、四个粒子 Fluid Demo、GPU/图像矩阵、性能或哈希；默认仍为 inline 且 Host 的完整 UI/资产/capture/feedback 帧派发按 8.6–8.9 继续迁移。

8.6 已完成，当前 **109/128**：新增 UI-owned `UiDrawPacket`，在主线程 `ImGui::Render` 后复制 draw lists 的顶点、索引与命令缓冲，清除 viewport/context 指针并拒绝可能回访可变 ImGui 状态的 user callback；D3D12/Vulkan backend 统一只接收 packet，不再调用 `ImGui::GetDrawData()`。`ImGuiSystem` 与 `EditorCoordinator` 已拆成 `BuildDrawPacket`/`Render(packet)`，Host 在 UI build 阶段冻结后才于原 UI draw 安全点消费；packet 保留 Scene/Game viewport `ITextureView` lease，且 `FrameEnvelope` 可持有同一只读 packet 供后续 execution service 派发。新增 `UiDrawPacketTests` 验证下一次 ImGui build 不改变旧顶点/索引/命令计数、纹理只在 packet 释放后回收；FrameQueue/UiDrawPacket/模块边界 3/3 与公开头检查通过，D3D12/Vulkan Editor headless preview 各 3 帧正常退出。windows-vulkan-only-ci 只构建 PrismApplication/PrismRender，不生成 PrismEditor/UiDrawPacket 目标并通过，no-Editor FrameQueue 直接通过。未运行 Demo 矩阵、四个粒子 Fluid Demo、图像、性能或哈希；下一项是 8.7 资产完成与 GPU 上传/绑定反馈迁移。

8.7 已完成，当前 **110/128**：AssetStreamingManager 将 IO 解码后的 GPU 创建/上传与 Registry 发布拆成两阶段；render execution lane 在原 BeginFrame 后安全点生成有序、不可变 binding update，`AssetRuntimeCoordinator` 以最多 4 个 batch 的有界反馈队列保活资源，主线程只在后续帧准备前按 sequence 应用 Published/Cleared revision。Material 依赖直接使用同一流送 entry 的已创建 Texture lease，不要求 render lane 读取主线程 Registry；eviction 在反馈应用前不清除 Registry，旧 packet/snapshot 的 shared lease 继续保活。`RenderRuntimeExecutionTarget` 统一承接 streaming work、manifest reload/reimport、环境贴图、Demo/glTF 工厂和 Demo 切换的 GPU 安全入口，Host 的资产/场景工厂路径已无直接 `GetGraphicsDevice()` 调用；显式切场景仍先 GPU drain 并由 target 通知 Coordinator。定向资产测试证明 2 个 render batch/6 个 binding 在主线程 apply 前不可见、apply 后 revision 有序发布，驱逐 apply 前旧 Material 可用、apply 后才清除，并覆盖取消和重复 Shutdown；完整/no-Editor 的 AssetRuntimeCoordinator、FrameQueue、公开头、模块边界均 4/4，通过 SceneSession/RenderScenePublication 2/2。D3D12/Vulkan 各一次 30 帧 headless streaming 冒烟正常退出。未运行全场景、四个粒子 Fluid Demo、图像/性能批次或源码哈希；下一项是 8.8 capture 与实际完成帧绑定。

8.8 已完成，当前 **111/128**：`CaptureAutomationController` 将截图 delay/sequence/streaming 请求绑定单调 request ID 与已接受 LogicalFrameId；只有 `RenderFrameCompletion` 才推进 completedFrames、完成 diagnostics/readback、写 RDG/GPU timing 报告及判断 max-frames/退出，生产 Host 不再自行 `++completedFrameCount` 或直接解析 backend capture。当前 inline 兼容路径构造与 service 相同的 acceptance/logical-frame/scene/data/epoch/settings 完成反馈，runtime target 的可靠 command 与直接路径共用 request/resolve 实现；GPU timing v2 保留原 capture 字段并新增完成身份。两种 Windows 构建的 FrameQueue fixture 覆盖一帧执行+一帧等待、resize/reset 后捕获、跨 scene epoch 延迟完成、错误顺序拒绝和失败/取消帧零成功 completion；公开头/模块边界通过。D3D12 Editor 与 Vulkan no-Editor 各 4 帧定向捕获均记录 `1,2,3,4`，delay=2 的旧 `recordedFrameId/source.frameId` 均为 2 且 `sourceFresh=true`；额外 Vulkan 2 帧运行证明 `EXIT_AFTER_CAPTURE`、RDG/GPU report 在实际完成帧触发，完成身份字段均为 frame/acceptance 2。完整 Editor 冒烟同时暴露 8.6 packet 将 ImGui backend 所需 OwnerViewport 置空；已改为 packet 自有最小 viewport，仅保留 renderer backend identity，不借用可变 ImGui viewport/context，UiDrawPacket fixture 与 D3D12 复测通过。未运行四个粒子 Fluid Demo、全场景、性能批次或源码哈希；下一项是 8.9 版本化可见性/统计反馈与主线程 UI 消费。

8.9 已完成，当前 **112/128**：`RenderExecutionService` 新增容量恒为一个 latest 值的 `RenderFrameFeedback` 通道，按实际完成 acceptance/logical-frame/scene/data/scene-view epoch/settings identity 携带每视图 `RendererStatistics` 副本和不可变 `RenderViewFeedback` lease；新统计覆盖旧统计而不阻塞或失败可靠 control/completion 通道。service 在发布前拒绝 scene generation、data revision、source ViewId 或未来 LogicalFrameId 不匹配的 visibility，记录拒绝数并只清空错误 payload，不回写 `RenderSceneData/RenderObject`。runtime target 与当前 inline 兼容路径都在帧完成后采集 Game/Scene 统计；Scene View 只消费与当前 packet 匹配的上一完成 Game visibility，EditorCoordinator 不再调用 live `gameRenderer.GetStatistics()`，只读取主线程保存的上一完成统计副本。两种 Windows 构建的 FrameQueue fixture 覆盖 latest replacement、四种身份拒绝及同时成功的可靠 control ack，公开头/模块边界通过；D3D12 Editor 三帧 headless preview 正常退出。未运行四个粒子 Fluid Demo、全场景、图像/性能批次或源码哈希；下一项是 8.10 drain、取消、任务收拢与亲和销毁。

8.10 已完成，当前 **113/128**：`RenderExecutionService::Shutdown()` 现在是可并发、幂等且可等待完成的关闭协议；首个调用关闭入口并排空已经接受的 frame/control，后续调用等待同一次 shutdown 完成，满队列中的阻塞生产者会被关闭或失败状态唤醒。可靠 `StopRenderExecutionCommand` 在指定帧边界完成确认后关闭接收端，同时保留已接受项的 FIFO drain；inline 执行失败也会将队列置为 Failed，从而取消未执行项并传播同一异常。执行 lane 仅调用一次 target shutdown，runtime target 仍按 coordinator/GPU drain 后销毁 renderer、最后释放 backend 的既有亲和顺序。两种 Windows 构建的 FrameQueue fixture 覆盖满队列 drain、阻塞生产者唤醒、并发/反复关闭、stop 边界、设备错误、部分初始化失败及 resize/control 队列情形，均通过；未运行 Demo 场景、图像/性能批次或源码哈希。下一项是 8.11 execution-mode 配置与默认 inline 回退。

8.11 进行中：service 已支持在队列为空且 startup target 已完成 inline 初始化时，一次性将 target 移交常驻 execution lane；移交与提交/关闭互斥，不重新创建 backend，不允许双 consumer，重复启动幂等。runtime target 会在新 lane 重新绑定线程亲和，之后 frame/control/shutdown 均留在同一线程。两种 Windows 构建的定向 fixture 已证明 `initialize/frame:1` 在 caller、`adopt/frame:2/shutdown` 在同一 resident lane。生产 FrameEnvelope 现已显式携带可选 capture ViewId，拒绝未激活视图；target 在 UI/present 前按 Game/Scene 最终输出记录已可靠接受的截图，并消费 UiDrawPacket。Editor 的 GPU draw、viewport descriptor 刷新和 GPU backend shutdown 已通过启动期回调归属 execution lane，Host 的三个重复 resize 刷新调用删除，关闭顺序保证 UI callback 在 backend 销毁前且 Editor owner 仍存活。ImGuiSystem 以单一窄锁覆盖 main-lane BeginFrame→packet 冻结区间，并与 render-lane draw/两个 viewport descriptor 原子替换/关闭互斥；异常路径可在 Host 等待 render shutdown 前释放未完成 build，锁不包围场景更新或 RenderGraph。EditorCoordinator 的逐帧输入已从两个 SceneRenderer 引用缩为 main-lane 可编辑 RenderSettings 与四个 viewport 尺寸值。Host 现进一步持有 producer-owned Game/Scene 设置、独立 history tracker 与输出尺寸镜像；Editor、无 Editor导航、capture action、Fluid 输入、Scene View 派生配置和 HPWater lifecycle 均只改该镜像，inline renderer 仅在冻结点接收兼容值复制。WaterValidation 所需的 WaterOptics pass/coverage 也已随完成反馈值化，不再回访 live RenderGraph/RendererStatistics。本轮把 Scene View 可见性/刷新决策移到帧发布之前，并将同一 `RenderFramePacket`、Game/Scene 设置值、逐视图 active 状态、UI packet、capture ViewId、scene/view epoch 与 settings revision 组合为完整且先行校验的生产 `FrameEnvelope`；当前 direct inline 渲染已消费该 envelope 的 packet/active/capture 决策，因此后续 `SubmitFrame` 接线无需重新计算或改变 Demo 输入。完整与 no-Editor 两个 Windows `PrismRender`/FrameQueue 目标编译通过，两套 FrameQueue 定向测试通过，D3D12 Editor/headless `preview` 2 帧正常退出。生产环境读取、以 service 执行该 FrameEnvelope、可靠 resize/quality/scene/capture control 和完成驱动循环尚未接线，因此 8.11 保持未勾选、整体仍为 **113/128** 且默认行为仍为 inline；未运行 Demo 矩阵、四个粒子 Fluid Demo、图像/性能批次或哈希。

8.11 后续推进：生产主循环已删除 `BeginFrame`、Game/Scene `Render`、UI draw、`EndFrame`、capture resolve 的重复直调，完整 `FrameEnvelope` 现在只通过 `RenderExecutionService::SubmitFrame` 执行并由 service 返回唯一 completion/feedback；截图在目标帧前作为可靠 `CaptureRenderFrameCommand` 提交，settings revision 变化作为 `ChangeRenderQualityCommand` 排序。execution target 同时回传 BeginFrame、Game/Scene、UI、Present 和 capture resolve 的 CPU 分段，Host 性能面板不再依赖重复执行路径。Windows Release `PrismRender`/FrameQueue 定向编译、FrameQueue fixture 与 D3D12 headless `preview` 2 帧通过。环境模式读取、threaded 下 resize/scene/shutdown/诊断等剩余 live target 访问仍需迁移，因此任务继续保持未勾选和 **113/128**；未运行其他 Demo、四个粒子 Fluid Demo、图像/性能批次或哈希。

8.11 本轮继续推进：`FrameEnvelope` 新增值化 profiling level 与 frame-pacing 开关，execution target 在所属 lane、`BeginFrame` 前应用；worker executor 的帧首/帧末累计统计和 `FramePacingStatistics` 由 target 复制进 latest completion feedback，Host 不再每帧直接设置 coordinator/backend 或读取其 worker/pacing 可变状态。FrameQueue fixture 同时覆盖配置到达 target 及统计/pacing 快照身份。由于公共帧载荷 ABI 变化，首次增量链接混入旧对象并在 clean shutdown 后触发 `0xC0000409`；完整 clean rebuild 259 个目标输入后，同一 D3D12 headless `preview` 1 帧、`waveworks-ocean` 3 帧及 FrameQueue fixture 均正常退出，确认不是渲染/WaveWorks 回归。任务仍为 **113/128**：`PRISM_RENDER_EXECUTION_MODE` 尚未接入生产启动，resize/scene switch/最终 drain/report 等 live target 访问仍需可靠迁移；未运行其他 Demo、四个粒子 Fluid Demo、图像/性能或哈希矩阵。

8.11 本轮继续推进：生产初始化现在严格解析并冻结 `PRISM_RENDER_EXECUTION_MODE=inline|threaded`，空值仍为 inline，非法值继续由既有 parser 拒绝；在剩余跨 lane 操作迁完前，threaded 请求会明确记录 `handoff_pending`，不会伪装成已启用。窗口 resize 已从 Host 对 target 的直调改为目标逻辑帧前的可靠 `ResizeRenderViewCommand`，每次 resize 单调推进 `viewEpoch`，同一 FrameEnvelope、quality/capture control 使用一致身份。execution target 进一步把 frame-context slot、逐视图 GPU timing generation/slot/timing list/timeline metadata 和可选 FrameDiagnostics 值化到 latest feedback；Editor 和 Host 的 profiling/diagnostics 不再为这些数据读取 live backend/renderer，启动期 adapter/API 也缓存为 main-lane 值。Windows Debug `PrismRender`/FrameQueue 目标构建和 FrameQueue fixture 通过，Release D3D12 headless `preview` 单帧正常退出；一次 Debug 实机启动在提交前命中既有 ImGui draw-list 内部断言并已终止，不作为渲染结果。任务仍为 **113/128**：threaded handoff 尚未开放，scene switch、legacy performance/RDG/GPU report、最终 asset pump/drain 与 shutdown 仍需迁移；未运行其他 Demo、四个粒子 Fluid Demo、图像/性能矩阵或哈希。

8.11 已完成，当前 **114/128**：性能资源计数、RDG/GPU timing 报告、最终 asset pump/drain/streaming report 均改由 execution lane 生成只读反馈或可靠 acknowledgement；Demo scene switch 和 editor import/reimport 也在下一逻辑帧边界通过类型化 control 执行，resize 在 UI 构建前先确认新 `viewEpoch`，避免同帧导入竞争。初始化完成后主线程缓存 adapter/API/性能 identity 并清空全部 live backend/coordinator/renderer/target 别名；显式 `threaded` 将同一已初始化 target 一次性交给常驻线程，空值/`inline` 保持默认且执行同一 FrameEnvelope/Control 路径。Release D3D12 headless `preview` 的 inline/threaded 各一帧正常退出，Release FrameQueue fixture 通过并覆盖 mode parser、lane handoff、scene/report/import acknowledgement；Debug 仅编译和 fixture 通过，单帧启动仍命中既有 ImGui draw-list 内部断言，未把它计作运行通过。未运行其他 Demo、四个粒子 Fluid Demo、图像/性能矩阵或哈希；8.12 才实现并证明 main N+1/render N 重叠，本项不宣称流水并行或 FPS 收益。

8.12 已完成，当前 **115/128**：FrameEnvelope 从主线程输入采样点携带单调时戳，队列记录 acceptance 时戳及接受时深度，execution feedback 按同一 acceptance/logical-frame/scene/data/view/settings 身份发布 queue wait、队列峰值与 input-to-Present 延迟。FrameProfiler/JSONL/UI 现在分开显示 Main active/wait、真实 Render lane active、Worker 聚合、完成等待、N+1 主线程重叠和端到端延迟，不再把主线程等待伪装成 Render CPU 时间；render target 与 Graph worker trace 使用各自 lane 类别。threaded 生产循环保留一个 completion work：提交 N 后直接进入 N+1 的事件、UI、场景提取、发布和 immutable FrameEnvelope 组装，只在提交 N+1 前收拢 N；inline 与显式性能记录路径仍立即收拢。FrameQueue fixture 使用无 sleep 的门闩固定 render N，在其释放前完成 N+1 immutable input 构造；两帧 completion 严格为 1/2、accepted=consumed=2、cancelled=0、峰值不超过一项等待，证明不是积压或丢帧。Release 定向测试通过，D3D12 headless `preview` 的 threaded+pool 两帧正常退出；未运行其他 Demo、四个粒子 Fluid Demo、图像/性能矩阵或哈希。下一项是 8.13 线程/控制/资产/UI 生命周期压力与 CPU 竞争检查。

8.13 已完成，当前 **116/128**：FrameQueue fixture 新增 128 个 threaded frame 与 128 条 after-frame 可靠控制交错压力，偶数控制实际走 asset-render-work acknowledgement；每帧 completion/latest feedback 身份匹配，消费后对应 immutable FramePacket weak lease 立即回收，最终 accepted=consumed=256、cancelled=0、waiting=0、peak waiting≤1。与已有满队列 producer 唤醒、设备失败、并发/重复 shutdown、control 取消、部分初始化失败门闩用例共同覆盖线程/命令生命周期；TaskScheduler 的 4096 任务/异常/嵌套等待、AssetRuntimeCoordinator 的 binding 保活/取消以及 UiDrawPacket 的纹理 lease 测试本轮 4/4 通过。自动化进程继续由 `Invoke-ArchitectureProcess` 有界 timeout 产生明确失败，不以 sleep 通过竞争；当前 Windows MSVC 环境仍无 TSan/等价 CPU race detector，如实保留为工具不可用。未运行 Demo、四个粒子 Fluid Demo、图像/性能或哈希矩阵。下一项是 8.14 代表性双后端执行模式门禁。

8.14 已完成，当前 **117/128**：沿用 P0 已封存 original evidence，本轮对当前 inline、inline+pool、threaded+pool 的 HPWater 在 D3D12/Vulkan 各运行 20 个确定性帧；以 inline 为同后端参考，D3D12 的 pool/threaded 均为 MAE 0.000011、RMSE 0.001252、changed 0.000075、SSIM 0.999983，Vulkan 均为 0.000014/0.001583/0.000100/0.999955，通过原门限。threaded+pool 另在两后端各运行 preview、shadows、terrain-vt、waveworks-ocean 三帧风险冒烟；明确未运行 `pbf`、`fluid-render`、`fluid-caustics`、`fluid-toon`。初版只预取事件的方案把 input-to-Present 从约 17.8 ms 拉到约 35 ms，已被拒绝并替换为完整 N/N+1 producer/completion 流水；替换后 D3D12 preview 60 帧双进程样本的 loop 中位约 17.07 ms、N+1 主线程重叠约 0.38 ms，端到端延迟约 34.19 ms，保留一帧线程滞后事实不宣称低延迟收益。UI GLFW 回调也纳入 ImGui 跨 lane 互斥；最终 D3D12 HPWater 连续五进程及 Vulkan 捕获通过，最终图像仍满足上述门限。没有运行全 Demo、四个粒子 Demo、全量 lifecycle、GPU validation 或源码哈希矩阵。下一项是 8.15 切换默认 threaded+pool，并保留显式 inline 后备。

8.15 已完成，当前 **118/128**：`ParseRenderExecutionMode("")` 默认返回 threaded，`ParseTaskExecutorKind("")` 默认返回 pool；显式 `PRISM_RENDER_EXECUTION_MODE=inline` 与 `PRISM_RENDER_TASK_EXECUTOR=inline` 继续走相同 FrameEnvelope/GraphExecutor 实现，不保留第二套渲染路径。解析/队列/任务池定向测试通过；无配置的 D3D12、Vulkan preview 与显式 D3D12 inline/inline preview 各三帧正常退出。默认切换发生在 8.13/8.14 保活、代表场景和图像门禁之后，shader、HPWater/WaveWorks 算法及 Demo 默认设置未改。下一项是 8.16 P7 协议/检查点记录。

8.16 已完成，当前 **119/128**：新增 `docs/RENDER_EXECUTION_PROTOCOL.md`，逐项记录 Main/Render/Worker owner、N/N+1 FrameEnvelope 数据流、可靠 control/ack、latest feedback、UiDrawPacket/asset lease、失败唤醒、drain/shutdown 和显式 inline 后备。P7 可重放检查点由 OpenSpec tasks、协议/验收文档、Release 定向构建和 `artifacts/architecture-refactor/p7-mode-matrix-20260831/` 证据组成；遵守用户要求不再复制整仓或生成逐文件 SHA-256。文档明确本 change 没有 RHI thread，也不把线程数量或单样本 FPS 表述为收益。下一项是 8.17 matched profiler/RHI-thread 决策。

8.17 已完成，P7 收口，当前 **120/128**：新增 `docs/RHI_THREAD_DECISION.md`。同一 FrameProfiler 的 D3D12 Preview matched 样本中，inline+inline/inline+pool/threaded+pool 的 producer interval 中位约 17.20/17.43/17.69 ms，threaded input-to-Present 约 34.9 ms，明确记录一帧 lag；threaded Render lane 约 6.5 ms active + 11.0 ms pacing wait。代表帧 BeginFrame 约 10.89 ms、Game/Scene render 约 0.63/5.66 ms、Graph build/execute 约 5.63/0.41 ms，而 submit+Present CPU 仅约 0.24 ms（native Present 0.14、submit 0.07）。API 翻译/提交不是可分离主瓶颈，因此本 change 不实现、也暂不建议独立 RHI thread；若未来满足 GPU 非饱和且 API translation 持续占关键路径，另立 change。下一项是 9.1 过渡接口/重复状态审计。

## 9. P8 — 完整回归、临时接口清理与最终交付

- [x] 9.1 审计临时适配器、重复状态、宽上下文、Host 回指与未迁移调用者；验证：清单逐项关闭，删除仅限已无调用者的过渡接口，full-rebuild 与 inline 诊断策略保留，main/核心算法/shader 无无关改动。
- [x] 9.2 运行最终 CPU/模块/边界/并发测试全套；验证：原有断言未削弱，新 Feature/快照规格逐条可追溯到测试，windows-ci 与 windows-vulkan-only-ci 两个 Windows 构建组合及头文件编译报告齐全。
- [x] 9.3 运行最终双后端 HPWater 图像与 lifecycle/temporal/队列/线程矩阵；验证：七视角、三质量、两尺寸和 view 切换、reset、resize、scene switch 满足 V2，同后端与跨后端门限分别判定，无新增 validation error/device removal。
- [x] 9.4 运行 Ocean/HPWater、GPU-driven terrain、流送、阴影/透明等风险代表 Demo 的适用编辑器/非编辑器固定图像与必要连续帧回归；明确排除 `pbf`、`fluid-render`、`fluid-caustics`、`fluid-toon`，不恢复全部 20 Demo 矩阵。验证场景切换、文件操作、undo/replay 和自动化退出/报告兼容，不降低代表路径画质。
- [x] 9.5 运行最终复制量、时长、线程排队/端到端延迟、内存/退役、PSO 分布对照；验证：V3 的零静态复制及性能门限通过，无丢帧或无界保活，不以 GPU 阶段和推算 FPS 或宣称未测性能收益。
- [x] 9.6 完成 `docs/RENDERER_ARCHITECTURE_CN.md`，说明 owner/target/data flow、线程交接/可靠控制、六类生命周期、Feature 接入和 mutation 发布范例；验证：示例与真实接口一致、关键路径可定位，明确哪些是保持行为的拆分、哪些是内部契约/数据流变化，说明 inline 回退。
- [x] 9.7 完成 `docs/ARCHITECTURE_REFACTOR_ACCEPTANCE.md` 的逐阶段证据、兼容性、已知警告及恢复说明；验证：每个阶段/规格场景都有实际报告链接，恢复流程可用，原 HPWater 证据未覆盖且未把其他 Ocean change 标成完成。
- [x] 9.8 审查全部任务与交付范围并运行 OpenSpec 严格校验；验证：没有未执行验证被勾选完成，`openspec validate refactor-renderer-architecture-boundaries --strict` 通过，只在所有实施/验收项完成后报告本 change 实施完成，归档另行请求。

5.2 已完成，当前 **68/128**：新增强类型 `SceneGeneration`、`RenderSceneDataRevision`、`RenderViewId` 与 `LogicalFrameId`，防止不同 revision/身份在新接口中隐式混用。`RenderSceneData` 只暴露 const 对象值；`RenderFrameDynamicData` 独立持有每逻辑帧共享灯光；`RenderView` 独立持有 current/previous camera、extent、history/cut revision 与基于稳定索引的选择；`RenderFramePacket` 以 `shared_ptr<const RenderSceneData>` 组合时间、动态块和多视图，旧 packet 可保留旧 revision。构造契约拒绝零 ID、空 scene data、非有限/负时间、空/重复/零尺寸 view、超容量灯光和越界对象选择。新增 `PrismRenderScenePublicationTests` 覆盖源值不可变、跨 packet 数据保活、双视图隔离、强 ID 与全部错误输入，直接测试通过；完整 `PrismRender` 构建链接通过。新类型尚未接管 mailbox/SceneRenderer，`WorldRenderSnapshot`、WorldSerializer 和文件格式均未编辑；未运行 Demo、GPU、性能或哈希。下一项是 5.3 稳定对象/父子/实例索引映射。

5.3 已完成，当前 **69/128**：新增 `RenderObjectId` domain 与 `RenderObjectIdentityRegistry`。World 对象身份直接保留 128-bit EntityId；程序化对象由 SceneGeneration 和稳定 key 分配，跨 data revision 保持，`ResetTopology` 只接受新的 generation 并重建程序化映射。Registry 同时为每个稳定 ID 保留 firstInstance；对象新增、删除与输入重排不会改变既有对象的 instance 映射。`BuildRenderSceneData` 两阶段解析所有身份和父引用，将 World/programmatic 父节点映射到当前共享数组索引，拒绝重复身份、缺失父节点、双重父声明和层级环；`RenderSceneData` 保存不可变 metadata 及 O(1) ID→index 映射。`BuildRenderViewSelection` 只发布共享数组索引，不复制或压缩对象。扩展 `PrismRenderScenePublicationTests` 验证新增/删除/重排、拓扑 generation 重建、World/programmatic 父子、Game/Editor selection，以及过滤前后 parentIndex/firstInstance 不变；直接测试通过，完整 `PrismRender` 链接通过。新模型仍未接管生产 mailbox，不影响现有 Demo；未运行 GPU、Fluid、场景矩阵或哈希。下一项是 5.4 CPU 参数值化与 AssetRegistry binding revision。

5.4 已完成，当前 **70/128**：`AssetRegistry` 的 mesh/texture/material 运行时槽在每次发布、替换或清除时取得全局单调 `RuntimeAssetBindingRevision`，新增 binding 查询同时返回原 handle、revision 与资源 lease，既有 `GetRuntime*` 和 32-bit handle 类型/序列化路径不变。`RenderSceneData` 为每个对象保存 `RenderSceneObjectAssetBindings`：mesh 与五个 texture 使用 const lease 保活，材质只发布 CPU `Parameters` 副本和各 texture binding，不暴露共享可变 Material；RenderObject 值中的 runtime mesh/material 指针在新快照构建时清空。底层 Texture/Mesh GPU 对象不深拷贝，仍由既有上传、frame-slot fence 与 retire 规则同步。定向测试验证 mesh/material/texture 替换、材质源对象原地改写、texture eviction 后旧快照均保持，revision 前进且 handle 数值/尺寸不变；`PrismRenderScenePublicationTests` 退出 0，`PrismRender` 完整链接通过。新快照仍未接管生产 mailbox，不影响 Demo；未运行 GPU、Fluid、场景矩阵、性能或哈希。下一项是 5.5 SceneChangeTracker 与提交范围。

5.5 已完成，当前 **71/128**：新增 `Engine::SceneChangeTracker`、`SceneChangeSet` 和 entity topology/hierarchy/transform/visibility/material/camera/lighting/full-rebuild 分类。CommandProcessor 的显式 transaction 使用 commit scope：命令成功后仅在 scope 内累积，commit 统一发布一个 revision，rollback 丢弃；独立 mutation、undo/redo、journal replay 和 world load/restore 通过相同分类入口，simulation mutation 不误标静态场景。SceneSession 不再维护重复 `m_worldDirty`，而是检查 committed change；手工未知写入记录 FullRebuild。`WorldRenderSceneBridge` 接收并回传 committed identity，成功同步后 Session 才消费，事务中间态不会进入 RenderScene。`PrismSceneSessionTests` 覆盖 transaction 原子性、五类对象变更、rollback、undo/redo/replay 和 bridge consume；`PrismWorldRenderSceneBridgeTests` 固定 revision/category/mutationCount 透传，两项均退出 0，`PrismRender` 链接通过。未运行 Demo、GPU、Fluid、性能、场景矩阵或哈希。下一项是 5.6 资产/场景激活 revision 接线。

5.6 已完成，当前 **72/128**：AssetRegistry 除槽内 revision 外新增 Published/Cleared binding change 记录，mesh/texture/material 的同步加载、reimport、streaming upload 与 eviction 都经既有 `SetRuntime*` 自动进入同一入口。SceneSession 消费这些事件并维护独立 binding identity；它不会误推 scene/data revision。成功 World bridge 提交只推进 data revision；`InitializeWorldFromRenderScene`（初始、Demo、streaming/programmatic 激活公共入口）推进 SceneGeneration 并将 data revision 复位为 1。RenderScene 新增 topology stamp，标准 Add/Clear/Replace 均推进；Demo factories、glTF loader、streaming bridge 已使用 Add/Clear，World bridge 与 SceneSerializer 改用 Replace，streaming activation 也显式回报 topologyChanged。未知外部 mutable 写入继续由 `MarkWorldChanged` 记录 FullRebuild。定向 `PrismAssetRuntimeCoordinatorTests` 验证 import/reload、streaming upload 和 budget eviction 的 Published/Cleared 事件；`PrismSceneSessionTests` 验证 scene/data/binding 三类 identity 正交；World bridge 测试保持通过，完整 `PrismRender` 链接通过。旧帧 lease 安全由 5.4 同一 fixture 固定。未运行 Demo、GPU、Fluid、性能、场景矩阵或哈希。下一项是 5.7 动态相机/灯光/设置输入。

5.7 已完成，当前 **73/128**：新增 session-owned `RenderDynamicInputState`，以固定 Game/Scene ViewId 保存逻辑帧、模拟时间、共享灯光 revision、当前/前一相机、camera/history/cut revision 和历史失效原因；每次场景 generation 切换重置该有界状态。`RenderHistorySettingsTracker` 在 Renderer 边界使用现有 WaterOptics dirty classifier 和视图管线字段生成单调设置 revision，Scene 不依赖 `RenderSettings`。ApplicationHost 在既有 BeginFrame 后、场景发布前统一采集 Editor、无 Editor 导航、验证脚本、resize、灯光与设置输入；WaterValidationSequence 的水下/水线切换显式发布 Game/Scene camera-cut 事件，算法与帧序列未改。World 的 Camera/Lighting-only 提交不再推进不可变对象 data revision。`PrismRenderScenePublicationTests` 验证双视图隔离、设置事件、灯光块、连续时间和水下相机；`PrismSceneSessionTests` 验证 camera/lighting-only revision 正交；两个测试及 `PrismRender` Release 构建/链接通过。未运行 Demo、GPU、Fluid、全场景矩阵、性能或哈希。下一项是 5.8 full-rebuild/versioned 提取策略。

5.8 已完成，当前 **74/128**：新增 `RenderSceneExtractor`，同一不可变数据构建路径提供 `full-rebuild` 与 `versioned` 策略；配置 `PRISM_RENDER_SCENE_PUBLICATION_MODE` 缺省/空值使用 full-rebuild，接受 `full-rebuild|versioned`，非法值启动时失败。Versioned 仅在 scene generation、data revision、runtime binding revision 与 topology revision 全匹配时复用同一个 `shared_ptr<const RenderSceneData>`；其他已知变更重建一次。FullRebuild 始终重建；FullRebuild change category 在 versioned 模式下返回 effective full-rebuild、`conservativeFallback=true` 和可见原因。SceneSession 已持有提取器但旧 mailbox/renderer 尚未切换，默认行为不变。定向测试覆盖默认/合法/非法配置、静态复用、transform 和 binding mutation 两策略等价、未知写入 fallback；两个 Scene 测试及完整 `PrismRender` Release 构建/链接通过。未运行 Demo、GPU、Fluid、场景矩阵、性能或哈希。下一项是 5.9 自洽 FramePacket mailbox。

5.9 已完成，当前 **75/128**：SceneSession 每帧从 extractor 取得不可变 scene data，并将其与 5.7 的逻辑帧、模拟时间、灯光和独立 Game/Scene RenderView 组装为自洽 `RenderFramePacket`。RenderSceneMailbox 在同一小互斥锁临界区只保存 latest envelope；envelope 同时持有 packet 与旧 RenderScene 适配值，5.10 前旧 SceneRenderer 消费路径保持。Host 已发布新 packet；full-rebuild 默认仍重建，显式 versioned 会跨连续帧复用相同 scene-data shared_ptr，统计区分 build/reuse 并记录一个小 packet。定向测试覆盖连续帧、双视图、scene generation 切换、旧 envelope 保活、无人持有后 packet/data 回收，以及连续 16 次发布不保留历史；Scene publication、SceneSession、World bridge 三项测试退出 0，`PrismRender` Release 构建链接通过。未运行 Demo、GPU、Fluid、场景矩阵、性能或哈希。下一项是 5.10 迁移 SceneRenderer/FeatureContext 消费 packet/view。

5.10 已完成，当前 **76/128**：新增只读 `RenderSceneView`，packet-backed 路径只持有 `shared_ptr<const RenderFramePacket>` 与稳定 ViewId，不复制共享对象数组；旧 `RenderScene` 仅保留为分阶段兼容适配。SceneRenderer、几何/透明 draw 构建、阴影、ClusteredLighting、GpuDrivenVisibility、PlanarReflections、LocalLightShadows、FluidFeature 与 FeatureContext 已改为从 view 解析相机、前帧相机、灯光、选择及不可变 mesh/material/texture binding lease。ApplicationHost 直接把同一 packet 分别交给 Game/Scene renderer，mailbox 生产路径不再保存 legacy scene 副本，publication/Game/Scene full-copy 计数归零。RenderGraph 与各 Feature graph callback 强持有 view；定向测试验证 packet view 不复制对象、地形策略不改稳定索引、原生异步并行录制期间 packet 保活且 graph reset 后释放。`PrismRender`、Scene publication 与 RenderGraph 目标构建通过，两项测试退出 0；D3D12 headless `preview` 仅运行 3 帧并正常退出。未运行 HPWater、四个 Fluid Demo、全场景矩阵、GPU validation、性能分布或哈希。下一项是 5.11 版本化每视图 GPU 可见性反馈与 Editor 辅助显示。

5.11 已完成，当前 **77/128**：新增不可变 `RenderViewFeedback`，每次 GPU visibility readback 固化来源 `SceneGeneration`、`RenderSceneDataRevision`、`RenderViewId`、`LogicalFrameId`、逐对象 reason 与实际 culling camera。Frame-slot 延迟回读仍不等待 GPU；Scene View 只接受与当前 packet 相同 scene/data 映射、来源为 Game View 且不来自未来逻辑帧的反馈。匹配反馈通过共享 metadata parent index选择地形 root/child，并驱动 terrain debug reason 与 `GameCameraFrustum` 变换；缺失或不匹配时使用保守 RootOnly 和当前 packet Game camera，不改写已发布 `RenderObject`。定向 Scene publication 测试覆盖匹配映射、旧 scene、旧 data revision、错误 ViewId、地形双策略、稳定父子索引、辅助视锥 culling camera 及源对象 reason 不变；Scene publication 与 RenderGraph 测试退出 0，`PrismRender` Release 构建链接和 D3D12 headless `preview` 3 帧冒烟通过。未运行 HPWater、四个 Fluid Demo、全场景矩阵、GPU validation、性能分布或哈希。下一项是 5.12 完整 mutation 矩阵和 mutable 入口审计。

5.12 已完成，当前 **78/128**：`RenderScene` 删除容易被非 const 对象误选的可写 `GetRenderObjects` 重载，仅保留只读访问；两个生产期原地写入点改用显式 `EditRenderObjectsForFullRebuild`，该入口推进 O(1) raw-mutation stamp。Versioned extractor 对未伴随 scene/data 提交的 raw write 与 topology write 分别报告 `raw-mutable-write-full-rebuild`/`untracked-topology-full-rebuild`，复用不扫描或哈希对象数组，并以 `sourceObjectVisitCount == 0` 直接测试。mutation fixture 覆盖 topology、hierarchy、transform、visible/editorOnly、mesh/material、camera/light、scene generation、unknown fallback 与 binding revision；旧 packet/view 在 runtime mesh/texture 替换后保留旧 lease，最后消费者释放后资源回收。SceneSession fixture 新增部分失败命令恢复和外层事务回滚，不增加状态哈希。生产 raw-write 审计及 D5 七类来源映射记录于 `docs/RENDER_SCENE_MUTATION_AUDIT.md`。仅构建/运行 `PrismRenderScenePublicationTests` 与 `PrismSceneSessionTests`，均通过；未运行 Demo、四个 Fluid Demo、GPU/性能/全场景或哈希矩阵。下一项是 5.13 full-rebuild/versioned GPU 对照与保活压力。

5.13 已完成，当前 **79/128**：D3D12/native 的 HPWater 60 帧同输入 A/B 中，full-rebuild 已完成 59 帧为 59 build/0 reuse，versioned 为 1 build/58 reuse，完整对象复制均为 0；末帧同后端比较通过（MAE 0.000046、RMSE 0.002147、changed 0.000245、SSIM 0.999884）。Terrain-VT 30 帧 A/B 为 29/0 对 1/28，末帧逐像素一致。只追加一条必要的 versioned HPWater lifecycle 最终门禁：330 个已完成帧为 4 build/326 reuse，覆盖历史 reset、168/180 resize、192/204/216 三次 Ocean 场景切换及 320 coverage 恢复；相对 P3 同输入 full-rebuild 参考通过（MAE 0.000033、RMSE 0.001617、changed 0.000119、SSIM 0.999927），D3D12 只有既有 warning 820。旧 packet CPU binding lease 的保活/最终 weak 回收由 5.12 fixture 覆盖，resize/场景切换经过完整 frame ring 后 clean shutdown 且无新增 GPU validation/lifetime 错误；并行 graph lambda 保活沿用 5.10 已通过的异步录制 fixture。未运行四个 Fluid Demo、全 Demo、Vulkan、性能双批或整仓哈希。下一项是 5.14 静态/相机/持续编辑复制与性能验收。

5.14 已完成，当前 **80/128**：D3D12/native `preview` 的 versioned 静态实机报告连续 120 帧为 1 build/119 reuse，后 119 帧 source object visit 为 0，publication/Game/Scene 完整对象复制和估算字节均为 0，FramePacket 小对象为每帧 1 个。`PrismRenderScenePublicationTests` 新增 120 帧仅相机输入和 120 个持续 Transform 编辑批次：相机路径为 0 build/120 reuse/0 source visit；每个真实编辑 revision 第一次恰好 rebuild、同 revision 第二次 reuse，未出现动态场景伪复用或重复 rebuild。单次结构阶段代表采样使用与 P0 相同的 D3D12/native、双视图、1280×800、可见不聚焦窗口条件，预热 180 后取得 180 个有效帧；相对匹配 P0 样本，CPU frame/loop 与 Game/Scene GPU Renderer 的 median、p95 均未触发 5%/10% 回退信号，V3 比较通过。为使证据显式记录快照策略，`Measure-ArchitecturePerformance.ps1` 增加可选 `ScenePublicationMode`；未运行双批、其他 Demo、四个 Fluid Demo、Vulkan 或额外哈希矩阵。下一项是 5.15 切换默认 versioned 并删除旧帧适配。

5.15 已完成，当前 **81/128**：空值/无环境配置和 `RenderSceneExtractor` 构造默认均切换为 `versioned`，显式 `PRISM_RENDER_SCENE_PUBLICATION_MODE=full-rebuild` 仍保留且与 versioned 的同 mutation 值/绑定等价 fixture 继续通过。D3D12/native `preview` 无 publication mode 环境变量的 120 帧实机报告为 1 build/119 reuse、完整对象复制与估算字节为 0。Mailbox 删除 `Publish(RenderScene)`、packet+legacy scene 双参数发布及公开 legacy envelope 获取；Host 只发布/取得同一不可变 packet，并直接保留发布 generation 用于既有诊断字段。SceneRenderer 删除逐帧 `RenderScene` Render 重载，GPU fixtures 改用共用 `RenderFramePacketFixture`；mutable scene 只以显式 `RenderSceneControlView` tag 用于初始化/SceneChanged 生命周期，不可误作帧输入。旧发布/渲染接口生产和测试调用检索为零；`PrismRender`、Scene publication、World bridge、两个受影响 GPU fixture 目标构建通过，两个 CPU fixture 直接通过。CLI、场景文件、统计格式 version 和报告字段未改；未运行 GPU fixture、其他 Demo、四个 Fluid Demo、Vulkan、双批性能或全仓哈希。下一项是 5.16 P4 验收记录与恢复检查点。

5.16 已完成，当前 **82/128**，P4 收口：`docs/ARCHITECTURE_REFACTOR_ACCEPTANCE.md` 已将版本化快照规格的 mutation、lease、双视图/反馈、零复制、HPWater/terrain 图像与 lifecycle、默认路径和 V3 单样本逐项映射到 5.3–5.15 的直接测试及隔离报告。运行时回退使用 `PRISM_RENDER_SCENE_PUBLICATION_MODE=full-rebuild`，保持同一 FramePacket/RenderView/Feature/RDG 消费路径；显式 full 与 versioned 的值/绑定及 5.13 GPU 对照已通过，删除环境变量恢复默认。阶段恢复点 `20260830-p4-versioned-scene-publication` 在创建流程内验证 986 个输入文件，manifest seal 为 `1CEA72DF40F4BE06A2D94BB597C4BDEF3A962A58C487DBAC4A5FC4C640AA72DE`；封存后只补记本任务和文档，不重复整仓哈希。OpenSpec strict 通过；未追加 Demo、四个 Fluid Demo、Vulkan、双批性能或 GPU fixture 运行。下一项是 6.1 CMake 模块边界清单。
