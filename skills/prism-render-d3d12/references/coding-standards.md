# Coding Standards

- Use `ComPtr` for COM resources
- Keep headers small
- Prefer one primary responsibility per class
- Avoid giant `Renderer` god objects
- Keep `main.cpp` minimal
- Name files after main classes where practical
- Preserve current architecture when extending features